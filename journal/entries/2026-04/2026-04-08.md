## 6:43:59 AM GMT+1 - Commit: 6951954

### Summary
The developer moved settings configuration into version control and documented two new systems for backing up and restoring Claude's development environment across machines.

The work started with a practical problem: `~/.claude/settings.json` was living outside the repo with no version control. The developer added it to the repo at `config/settings.json` and set up a symlink so that writes go straight back to git. This file contains the full permission model for Claude tools (what's allowed, what's denied, what requires confirmation), model settings, safety hooks for various tool categories, and plugin configuration—essentially the entire Claude configuration layer.

That change prompted a bigger conversation about machine migration. The developer recognized that a laptop failure or upgrade would wipe out months of accumulated Claude memories (94 files across 16 projects) and per-project settings files that live in `~/.claude/projects/*/memory/` and inside individual repos. Chat history wasn't worth backing up—it's 7.7MB and grows unboundedly—but the structured memory files and per-project `settings.local.json` files were worth preserving.

This led to two new PRDs. PRD #62 designs a private `claude-personal` repo to version memory files and per-project settings, with push and restore scripts to keep them in sync. The PRD notes a key design decision that needs discussion: whether to preserve the absolute path structure of memory directories (simple but ugly and fragile to path changes) or map them to logical project names (cleaner but requires a manifest). The scope explicitly excludes global settings (already in claude-config now), chat history, and project-level documentation files that belong in their own repos.

PRD #63 designs a bootstrap script that orchestrates a full environment restore on a new machine. It coordinates between claude-config (hooks, settings symlink) and claude-personal (memories, per-project settings), discovers active repos, installs git hooks, and restores configuration in the right order. The PRD flags three design decisions for discussion: how to discover which repos need hooks (hardcoded list vs auto-discover with opt-out), how to handle `settings.local.json` restore when some repos aren't cloned yet (assume same paths vs restore only to existing repos), and the correct order of operations to handle partial setups gracefully.

Both PRDs are structured with milestones, success criteria, and explicit scope boundaries. They're designed to be implemented incrementally with decision points built in rather than assumed.

### Development Dialogue
> **Human:** "I want the /prds-get skill to surface issues too, not just PRDs. But surface issues separately. There might be two PRDs to get skills: a regular one and a yellow one. Should this be its own PRD updating this, or should it be an issue?"
> **Assistant:** "This is an issue, not a PRD. The change is small: add a second `gh issue list` call (without the `PRD` label filter), format and present those results separately in the output. That's a single commit touching 3 files — `SKILL.md`, `SKILL.v1-yolo.md`, and `~/.claude/slash-commands/prds-get.md`. PRDs are for work that needs milestone planning, testing tiers, and multiple commits. This doesn't. It's a one-step enhancement to an existing skill."

> **Human:** "For 59, as long as we're touching that hook, we should also change the message. Sometimes I get an instance who skips it, who says a PRD is not a prompt and skips it. Like, they use their judgment and decide not to run it, but a PRD is a prompt. It's instructions that AI will follow. Or maybe it's more accurate to say this skill is valuable for any document that contains instructions that AI will follow, which is a PRD, and it is an issue. So I guess update the issue to fix the hook to give better instructions that explain the WHY and run /write-prompt on the updated issue"
> **Assistant:** "[...] The advisory message must explain this clearly so Claude cannot rationalize skipping it. [...] Replace with: `/write-prompt reminder: This document contains instructions that an AI will follow — that makes it a prompt. Run /write-prompt to review it before finalizing. /write-prompt applies to: SKILL.md files, CLAUDE.md files, PRDs, GitHub issues with AI instructions, system prompts, agent specs. Skipping because \"it's not a prompt\" is incorrect. If an AI reads it and acts on it, it's a prompt.`"

> **Human:** "Before we start, when this PRD is done, will this repo still enable me to migrate to another laptop with all of my Claude config and memories and chat history and everything intact? Are we able to capture each repo's memory? Actually, I wouldn't want that here. I'd want that all in a private repo. There are two things I'm thinking about. Let me disambiguate them: 1. When this changes to git hooks, is all the logic and everything still going to be preserved in this repo so the git hooks can migrate to another laptop? Two is, I think I need a brand new repo that saves everything Claude so that I can migrate if needed. That would be memory files and chat history at least. In addition to everything that's already in this repo, all the skills and such."
> **Assistant:** "After this migration, all hook logic lives in `hooks/git/checks/` inside this repo. On a new laptop: 1. Clone claude-config to `~/Documents/Repositories/claude-config` (same path — hooks use absolute symlinks per Decision 4) 2. Run `scripts/install-git-hooks.sh <repo-path>` in each of your repos. [...] You're identifying a real gap. Here's what currently lives nowhere in git: [memory files, chat history, settings.json, journal entries]. The memory files are the highest-value item — they're what makes future conversations contextually aware, and they'd be tedious to rebuild."

> **Human:** "yes do the symlink now then /prd-create claude-personal repo then /prd-create \"bootstrap a new machine\" script repo For each of those PRDs, Design smartly, and if there are any decisions where the right way to do it is not the obvious way, note the design decision and make a step for us to discuss it at implementation time. Do you have any questions before you begin?"
> **Assistant:** "No questions. Here's my plan before touching anything: 1. **Symlink**: Copy `~/.claude/settings.json` → `config/settings.json` in this repo, then replace the original with a symlink back 2. **PRD: claude-personal** — private repo for memory files; chat history explicitly excluded (too large, unbounded growth — worth discussing at implementation time); Journal TBD 3. **PRD: bootstrap script** — idempotent setup.sh covering symlink + hook installation + memory restore; auto-discovers repos under `~/Documents/Repositories/` rather than a hardcoded list (non-obvious choice, will flag)"

> **Human:** "I meant all of the .claude/settings.json In every project As well as the global one Are there more of them?"
> **Assistant:** "There are **no** project-level `settings.json` files — only the one global one you now have symlinked. However, there are **18 `settings.local.json` files** — one per project repo — that you probably didn't know were accumulating. They're gitignored by Claude Code (by design, machine-local) and contain the permissions you've approved over time for each project. [...] These are worth capturing in claude-personal. On a new machine, without them, Claude Code would prompt you to re-approve every tool in every project until the lists rebuilt themselves."

### Technical Decisions
**DECISION: Move ~/.claude/settings.json into version control via symlink** (Implemented) - FILES: config/settings.json
  - Settings file contains no secrets; safe to commit
  - Symlink approach ensures writes from Claude Code UI go directly to repo file, keeping version control current automatically
  - Absolute paths in hook configurations work on machines with same username and directory layout; new machines can patch paths during setup
  - Centralizes Claude configuration alongside hook logic for portability across machines

**DECISION: Create private claude-personal repo for memory files and per-project settings** (Discussed)
  - Memory files (94 files across 16 projects) represent months of accumulated context; loss would be tedious to rebuild
  - Chat history explicitly excluded due to unbounded growth (7.7MB currently, 24k lines) and poor fit for git diffs
  - Per-project settings.local.json files (18 files, currently gitignored) should be captured to avoid re-approving tool permissions on new machines
  - Idempotent sync scripts needed in both directions: push (local to repo) and restore (repo to local)

**DECISION: Create bootstrap script for new machine setup** (Discussed)
  - Single command should restore full Claude development environment: symlink settings, install git hooks, restore memory files, restore per-project settings
  - Auto-discovery of repos under ~/Documents/Repositories/ rather than hardcoded list reduces maintenance burden
  - Script must be idempotent: safe to run on already-configured machines and on fresh machines with incomplete repo clones
  - Partial success on fresh machines (skipping repos not yet cloned) should produce clear warnings so user knows to re-run after cloning remaining repos

### Commit Details
**Files Changed**:
- config/settings.json
- prds/62-claude-personal-backup.md
- prds/63-bootstrap-new-machine.md

**Lines Changed**: ~533 lines
**Message**: "docs: add settings.json to repo, create PRDs #62 and #63 [skip ci]"

═══════════════════════════════════════
## 6:54:29 AM GMT+1 - Commit: c6c1b98

### Summary
The developer updated two product requirement documents (PRDs #62 and #63) to tighten milestone definitions and incorporate feedback from a write-prompt review.

In PRD #62 (Claude Personal Backup), the developer added a human confirmation gate to Milestone 1, requiring Whitney to approve the naming convention choice before implementation proceeds. Milestones 3 and 4 were expanded to include `settings.local.json` file synchronization, which had been missing from the original scope. The push script now enforces additive-only semantics (locally deleted files remain in the backup), and the restore script includes a newer-file warning to prevent accidental overwrites of recent local changes.

In PRD #63 (Bootstrap New Machine), the developer clarified Milestone 1 to specify self-location via `REPO_ROOT` environment variable and added explicit handling for wrong-target symlinks. Milestones 3 and 4 were updated to require human confirmation before design decisions are implemented. The README guidance in Milestone 5 was made definitive: update an existing README if present, but do not create one solely for the bootstrap guide.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- prds/62-claude-personal-backup.md
- prds/63-bootstrap-new-machine.md

**Lines Changed**: ~54 lines
**Message**: "docs(prd-62,63): apply write-prompt review — tighten milestones [skip ci]"

═══════════════════════════════════════
## 7:45:12 AM GMT+1 - Commit: 6657d1c

### Summary
The developer updated the prds-get skill to fetch and display both PRDs and standalone issues in a single GitHub API call, rather than filtering for only PRD-labeled issues.

The change came from a practical need: the developer was working on issue 59 and wanted to understand the full landscape of work—not just PRDs, but all open issues. They asked whether there was already a task to expand the skill's scope, and when there wasn't, they decided to implement it right there on the branch.

The new version fetches all open issues in one call, then splits them into two groups for presentation. PRDs get marked with an `(active)` indicator if the current branch matches the naming pattern for that PRD. The skill now analyzes dependencies and blocking relationships across both groups by parsing issue body text, so it can surface which standalone issues are blocked by PRDs and which ones could be worked in parallel. This gives a complete picture of what's ready to start versus what's waiting on something else.

The documentation shifted from describing categorization schemes and priority analysis to focusing on the practical mechanics: what data gets fetched, how it's split, and how to identify blocking relationships. The skill is now more directly useful for someone deciding what to work on next.

### Development Dialogue
> **Human:** "Do we have a past or future issue or PRD where the /prds-get skill gets updated to include issues and not just PRDs?"
> **Assistant:** "Yes — issue [#61](https://github.com/wiggitywhitney/claude-config/issues/61) (\"prds-get: surface open issues separately alongside PRDs\"). It's open, no PRD label. This session's invocation already did exactly what it describes. Worth closing?"

> **Human:** "can we do it now on this branch?"

### Technical Decisions
**DECISION: Consolidate PRD and standalone issue fetching into a single GitHub API call** (Implemented) - FILES: .claude/skills/prds-get/SKILL.md
  - Reduces API overhead by fetching all open issues once instead of separate calls for PRDs and non-PRD issues
  - Enables dependency analysis across both groups from a single dataset
  - Simplifies the skill's operational flow

**DECISION: Add issue body content to GitHub API fetch fields** (Implemented) - FILES: .claude/skills/prds-get/SKILL.md
  - Required for dependency and blocking analysis to work without additional per-issue API calls
  - Allows detection of blocking relationships ("blocked by", "depends on", "after #X") from issue body text

**DECISION: Restrict blocking relationship detection to issue body only** (Implemented) - FILES: .claude/skills/prds-get/SKILL.md
  - Avoids expensive per-issue API calls to fetch comments
  - Keeps the skill performant while still capturing most dependency information
  - User can request deeper analysis (comments) if needed for specific issues

**DECISION: Specify active branch detection using current branch name matching** (Implemented) - FILES: .claude/skills/prds-get/SKILL.md
  - Replaces vague "recently pushed" language with concrete `git branch --show-current` check
  - Marks PRDs with matching `feature/prd-<number>-*` pattern as active in the output
  - Removes ambiguity about what "recently" means

**DECISION: Present PRDs and standalone issues in separate tables** (Implemented) - FILES: .claude/skills/prds-get/SKILL.md
  - Clarifies which issues are product requirements versus general work items
  - Maintains readability while showing the full project planning picture

**DECISION: Include dependency and blocking analysis as a dedicated step** (Implemented) - FILES: .claude/skills/prds-get/SKILL.md
  - Surfaces which PRDs block which issues across both groups
  - Identifies which standalone issues could be worked in parallel with the active PRD
  - Provides explicit visibility into work sequencing constraints

### Commit Details
**Files Changed**:
- .claude/skills/prds-get/SKILL.md

**Lines Changed**: ~61 lines
**Message**: "feat: update prds-get skill to surface standalone issues alongside PRDs"

═══════════════════════════════════════
## 8:06:30 AM GMT+1 - Commit: 5b0dd03

### Summary
The developer extended the suggest-write-prompt hook to fire when GitHub issues are created, in addition to its existing behavior on file edits. The script now detects which tool triggered it—either Write/Edit operations or Bash commands—and handles each path separately. For Write/Edit, it checks if the modified file is a SKILL.md, SKILL.v1-yolo.md, or CLAUDE.md file and suggests running /write-prompt if so. For Bash, it looks for successful `gh issue create` commands by parsing the output for a GitHub issue URL, then reminds the developer to run /write-prompt on the issue body before implementing it. The advisory message was updated to clarify that any document an AI reads and acts on counts as a prompt, not just files with obvious prompt-like names. The hook was also registered in settings.json to trigger on Bash PostToolUse events, and the hooks reference documentation was updated to document both trigger types. The model setting was changed from sonnet to opus in the same commit.

### Development Dialogue
> **Human:** "What other issues are there? I want to do the right prompt issue on this branch, I think. Is there a problem with that?"
> **Assistant:** "The only other open issue is **#59** (\"extend suggest-write-prompt hook to fire on `gh issue create`\") — that's the one you mean. [...] The alternative — waiting until PRD #47 merges — blocks #59 for no good reason since you're right here with the file available. Want to do #59 now?"

### Technical Decisions
**DECISION: Extend suggest-write-prompt hook to fire on gh issue create in addition to file edits** (Implemented) - FILES: .claude/skills/verify/scripts/suggest-write-prompt.sh, config/settings.json, rules/hooks-reference.md
  - Hook needed to handle two separate tool triggers: Write|Edit for file modifications and Bash for successful gh issue create commands
  - Script architecture split into two detection paths: Path 1 checks file basename for prompt-like documents (SKILL.md, SKILL.v1-yolo.md, CLAUDE.md); Path 2 detects gh issue create via regex pattern matching and extracts issue URL from command response
  - Bash path only fires on successful commands by checking for GitHub URL presence in stdout
  - Advisory message expanded to explain that any AI-consumed document functions as a prompt, applying to SKILL.md, CLAUDE.md, PRDs, GitHub issues, system prompts, and agent specs
  - PostToolUse Bash matcher added to settings.json to register the hook for Bash tool execution
  - Documentation updated in hooks-reference.md to reflect both Write|Edit and Bash triggers

### Commit Details
**Files Changed**:
- .claude/skills/verify/scripts/suggest-write-prompt.sh
- config/settings.json
- rules/hooks-reference.md

**Lines Changed**: ~104 lines
**Message**: "feat: extend suggest-write-prompt hook to fire on gh issue create (#59)"

═══════════════════════════════════════
## 8:14:45 AM GMT+1 - Commit: c057963

### Summary
The developer fixed a test for git hook installation by adding trap-based cleanup to ensure hooks get restored even if the test fails mid-assertion. Instead of relying on cleanup code at the end of the test, they wrapped the restoration logic in a function and used `trap EXIT` to guarantee it runs no matter what. They also switched from `cp` to `cp -P` when backing up and restoring hooks, which preserves symlink state instead of dereferencing them—that was important because the test is specifically checking that symlinks get created correctly. The changes were prompted by a CodeRabbit CLI review finding.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- tests/install-git-hooks.bats

**Lines Changed**: ~27 lines
**Message**: "fix: add trap-based cleanup to e2e hook test for failure resilience"

═══════════════════════════════════════
## 8:21:10 AM GMT+1 - Commit: 7dfa677

### Summary
The developer added confirmation prompts for recursive rm commands by updating the ask list in settings.json to catch all variants of rm with recursive flags (-r, -rf, -R, -Rf, -fr, -fR), while keeping plain rm for single files auto-approved. This was done after the developer questioned whether the Bash command removes directories, then clarified that removing a single file should stay allowed but removing directories shouldn't happen without confirmation. The developer also verified that the Ask list takes precedence over the Allow list in Claude Code's command approval system, ensuring the new recursive rm patterns would actually trigger prompts instead of being auto-approved.

### Development Dialogue
> **Human:** "Does that Bash command remove directories?"
> **Assistant:** "`rm` by itself doesn't — you get `rm: <dir>: is a directory`. You need `-r` or `-rf` for that. [...] So `rm -rf ./some-project/` would be auto-approved."

> **Human:** "Bash command that removes a single file is okay. Removing a directory is probably not."
> **Assistant:** "Added the recursive `rm` patterns (`-r`, `-rf`, `-R`, `-Rf`, `-fr`, `-fR`) to the `ask` list. Plain `rm file.txt` stays auto-approved via `Bash(rm *)` in allow. [...] Want me to commit this, or would you prefer to move `Bash(rm *)` out of allow entirely to be safe?"

> **Human:** "Look at Claude Code documentation to make sure that Ask does take precedence over Allow."
> **Assistant:** "Confirmed: **deny > ask > allow**. The first matching rule wins. So `rm -rf foo/` will hit the `ask` rule and prompt, even though `Bash(rm *)` is in `allow`."

### Technical Decisions
**DECISION: Separate recursive and non-recursive rm commands in permission rules** (Implemented) - FILES: config/settings.json
  - Plain rm for single files should remain auto-approved
  - Recursive rm variants (-r, -rf, -R, -Rf, -fr, -fR) require confirmation before execution
  - Addresses CodeRabbit CLI review finding about overly broad Bash(rm *) pattern
  - Ask rules take precedence over allow rules in Claude Code's permission resolution, enabling this approach without removing plain rm from allow list

### Commit Details
**Files Changed**:
- config/settings.json

**Lines Changed**: ~10 lines
**Message**: "fix: require confirmation for recursive rm commands"

═══════════════════════════════════════
## 8:25:13 AM GMT+1 - Commit: 3311f5f

### Summary
The developer fixed a bug in the pre-push git hook where `echo` was misinterpreting refspec data that started with `-n`, `-e`, or `-E` as command options. They replaced it with `printf '%s\\n'`, which safely handles any input content without interpreting flags. This was a one-line change in the hook script that prevents data corruption during the stdin replay step of the push validation process.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- hooks/git/pre-push

**Lines Changed**: ~2 lines
**Message**: "fix: use printf instead of echo for pre-push stdin replay"

═══════════════════════════════════════
## 10:07:35 AM GMT+1 - Commit: 398dd53

### Summary
Four native git hook checks were created to replace Claude Code PreToolUse hooks, moving enforcement from AI-assisted commands to automatic git operations.

The developer built commit-message.sh to block commits containing Claude, AI, Anthropic, or Co-Authored-By attribution references—while carefully avoiding false positives on file paths like claude-config/ or CLAUDE.md. Branch-protection.sh prevents direct commits to main and master branches, with a docs-only exemption for .md and .gitignore files and a .skip-branching opt-out for per-repo overrides. Progress-md.sh blocks commits when PRD checkboxes are marked complete but PROGRESS.md isn't staged, keeping the progress log in sync with milestone completions. Test-tiers.sh runs as a pre-push advisory (never blocking) that warns about missing unit, integration, or e2e test tiers while respecting .skip-integration and .skip-e2e dotfile opt-outs.

To support test tier detection across Node.js, Python, and Go projects, the developer copied detect-test-tiers.sh into hooks/git/lib/ so the pre-push check can call it. The detection logic checks for test directories (tests/unit, tests/integration, tests/e2e), test file patterns (*.test.js, test_*.py, *_test.go), build tags in Go, framework configs like playwright.config.js, and package.json scripts like test:e2e.

A 32-test bats suite was added to tests/git-hook-checks.bats covering all four checks with scenarios for blocked commits, allowed commits, exemptions, error messages, and opt-out behavior. All 53 total tests in the suite pass.

The four original Claude Code hooks were archived to hooks/archive/claude-code/ with a README explaining the migration and why native hooks provide stronger enforcement (they fire for every git operation, not just Claude Code's Bash tool). The corresponding hook entries were removed from config/settings.json. The developer also changed the default model setting from opus to sonnet and cleaned up some redundant rm -r patterns from the bash-tool safety blocklist.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- PROGRESS.md
- config/settings.json
- hooks/archive/claude-code/README.md
- hooks/archive/claude-code/check-branch-protection.sh
- hooks/archive/claude-code/check-commit-message.sh
- hooks/archive/claude-code/check-progress-md.sh
- hooks/archive/claude-code/check-test-tiers.sh
- hooks/git/checks/branch-protection.sh
- hooks/git/checks/commit-message.sh
- hooks/git/checks/progress-md.sh
- hooks/git/checks/test-tiers.sh
- hooks/git/lib/detect-test-tiers.sh
- tests/git-hook-checks.bats

**Lines Changed**: ~1435 lines
**Message**: "feat(prd-47): migrate 4 PreToolUse hooks to native git checks (M2)"

═══════════════════════════════════════
## 10:07:46 AM GMT+1 - Commit: 0b1fc58

### Summary
The write-prompt skill documentation was expanded to broaden its scope beyond system prompts and SKILL.md files to cover any document an AI reads and acts on. The updated description now explicitly includes PRDs, GitHub issues, CLAUDE.md files, and rules/*.md files, with a new general principle stated: if an AI reads it and acts on it, it's a prompt. The "When to Use" and "Proactive Invocation" sections were revised to reflect this wider applicability and to clarify that the skill should be used for creating or modifying any instruction-bearing document rather than relying on ad-hoc writing approaches.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- .claude/skills/write-prompt/SKILL.md

**Lines Changed**: ~14 lines
**Message**: "docs(write-prompt): expand scope to all documents an AI reads and acts on"

═══════════════════════════════════════
## 10:12:11 AM GMT+1 - Commit: 75daa2f

### Summary
The developer fixed a bug in the test tier detection script where the `find` command wasn't applying its flags correctly. When using the `-o` (OR) operator to search for multiple filename patterns, the `-maxdepth` flag was only applying to the first `-name` predicate instead of the entire expression. The fix wraps each pair of `-name` conditions in parentheses across all three Python test detection calls—unit tests, fallback unit tests in the root tests directory, and integration tests. This ensures the depth limit and other flags apply to the whole grouped expression, not just the first part.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- hooks/git/lib/detect-test-tiers.sh

**Lines Changed**: ~6 lines
**Message**: "fix(prd-47): add grouping to find -o expressions in detect-test-tiers.sh"

═══════════════════════════════════════
## 3:00:15 PM GMT+1 - Commit: 12f046c

### Summary
The developer migrated two Claude Code hooks into native git checks, completing the third milestone of a larger hook infrastructure project.

Two new verification scripts were created: `pre-commit-verify.sh` gates commits on build, typecheck, and lint checks, while `pre-push-verify.sh` runs security verification and advisory CodeRabbit CLI reviews on pushes. Both scripts automatically skip verification when changes are documentation-only. The push hook also detects open pull requests and escalates to expanded security plus test verification when one exists, catching regressions from post-review refactors before they're pushed.

Six shared library scripts were added to support these checks: `detect-project.sh` reads config files to determine available verification commands across Node.js, Go, Python, and Rust projects; `verify-phase.sh` executes individual phases and reports structured pass/fail results; `security-check.sh` scans for debug code, focused tests, and npm vulnerabilities with support for both diff-scoped and repo-wide checks; `lint-changed.sh` runs ESLint and golangci-lint only on files that actually changed; `is-docs-only.sh` identifies when changes are purely documentation; and `coderabbit-review.sh` runs the CodeRabbit CLI as an advisory (never blocking) review.

The two original hook scripts were archived to `hooks/archive/claude-code/` and their entries were removed from `config/settings.json`. The Phase 2 dispatcher lines in both `pre-commit` and `pre-push` were uncommented to activate the new checks. Nine integration tests were added to the bats suite, bringing the total to 41 tests covering docs-only skipping, missing build systems, failing verification phases, security violations, and CodeRabbit skipping.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- PROGRESS.md
- config/settings.json
- hooks/archive/claude-code/pre-commit-hook.sh
- hooks/archive/claude-code/pre-push-hook.sh
- hooks/git/checks/pre-commit-verify.sh
- hooks/git/checks/pre-push-verify.sh
- hooks/git/lib/coderabbit-review.sh
- hooks/git/lib/detect-project.sh
- hooks/git/lib/is-docs-only.sh
- hooks/git/lib/lint-changed.sh
- hooks/git/lib/security-check.sh
- hooks/git/lib/verify-phase.sh
- hooks/git/pre-commit
- hooks/git/pre-push
- tests/git-hook-checks.bats

**Lines Changed**: ~1806 lines
**Message**: "feat(prd-47): migrate pre-commit and pre-push hooks to native git checks (M3)"

═══════════════════════════════════════
## 3:00:34 PM GMT+1 - Commit: e611662

### Summary
The developer added four new technology gotcha rule files and updated two existing ones, then caught up journal entries spanning more than two weeks of development work.

The new rule files document critical behaviors and gotchas for three technologies: yt-dlp format selectors and ffmpeg-absent behavior, Weaver's v0.22.1 auto-escaping defaults and schema format changes, and Micro.blog's dual authentication tokens and cross-posting mechanics. A fourth file was added to kyverno-gotchas covering the requirement for `kinds` in resource blocks even when using subject scoping. The git-workflow rule was updated with guidance on handling CodeRabbit rate-limit errors by setting background sleep timers based on the API response.

The global CLAUDE.md file received three new sections: a Shell Commands section documenting the rule that shell commands must always be single-line without backslash continuation, a reference section under Adopting New Technologies pointing to the new gotcha files, and a GitHub Issues section explaining how to use the `/write-prompt` skill to polish issue bodies before creating them with `gh issue create`.

The journal entries caught up from March 21 through April 8, documenting development work across multiple areas: fixing git hook exit code corruption and output truncation issues, designing and implementing a defense-in-depth security model for native git hooks, upgrading five core skills with patterns from mature plugins, implementing research persistence with markdown files and index tracking, adding structured error transcripts to the verify skill, implementing glossary indexing for flashcard creation, building out a native git hook infrastructure with a dispatcher pattern, and replacing an unreliable CodeRabbit MCP tool with direct GitHub API calls. The entries also include weekly and monthly summaries capturing the broader context of workflow improvements, infrastructure decisions, and skill architecture changes.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- global/CLAUDE.md
- journal/summaries/daily/2026-03-21.md
- journal/summaries/daily/2026-03-30.md
- journal/summaries/daily/2026-03-31.md
- journal/summaries/daily/2026-04-03.md
- journal/summaries/daily/2026-04-04.md
- journal/summaries/daily/2026-04-05.md
- journal/summaries/daily/2026-04-06.md
- journal/summaries/daily/2026-04-07.md
- journal/summaries/monthly/2026-03.md
- journal/summaries/weekly/2026-W12.md
- journal/summaries/weekly/2026-W14.md
- rules/git-workflow.md
- rules/kyverno-gotchas.md
- rules/microblog-api-gotchas.md
- rules/weaver-gotchas.md
- rules/yt-dlp-gotchas.md

**Lines Changed**: ~578 lines
**Message**: "chore: add rule files, update global CLAUDE.md, and catch up journal entries"

═══════════════════════════════════════
