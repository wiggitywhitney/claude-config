## 6:51:31 AM GMT+1 - Commit: 7d655cb

### Summary
The developer corrected the scope documentation for the M4 rollout phase of a native git hook migration project. Two clarifications were made: `~/Documents/Journal` is a standalone repository outside the main Repositories directory and requires no special approval gate, and `spinybacked-orbweaver` is a closed/inactive repository that should be treated like any other repo during installation. These corrections superseded the previous decision that had called for per-repo approval gates for these specific locations, allowing all repositories to be installed in a single batch pass instead.

### Development Dialogue
> **Human:** "Journal/spinybacked-orbweaver isn't a thing

there's Documents/Journal (check on this)

and Repositories/spinybacked-orbweaver (this is closed now treat it like any other repo)"

### Technical Decisions
**DECISION: Rollout Scope Corrections for M4 Milestone** (Discussed) - FILES: prds/47-native-git-hook-migration.md
  - `~/Documents/Journal` is a standalone git repo outside `~/Documents/Repositories/` and requires no special approval gate
  - `spinybacked-orbweaver` is closed/inactive and should be treated like any other repo without special approval gate
  - Decision 7's approval gate is fully superseded by this correction
  - All repos including Journal and spinybacked-orbweaver can be installed in a single batch pass without per-repo confirmation

### Commit Details
**Files Changed**:
- prds/47-native-git-hook-migration.md

**Lines Changed**: ~9 lines
**Message**: "docs(prd-47): amend M4 rollout scope — remove approval gates"

═══════════════════════════════════════
## 6:53:32 AM GMT+1 - Commit: 6c5bb4c

### Summary
The developer fixed a critical bypass in the gog CLI safety hook by updating all command pattern matching to account for the `-a/--account` flag that appears between `gog` and subcommands. The original patterns used `\bgog\s+<subcommand>` which failed when the account flag was present, allowing destructive operations to slip through every safety check.

The fix changed all pattern lists—OUTREACH_COMMANDS, CALENDAR_WRITE_COMMANDS, CALENDAR_PEOPLE_COMMANDS, SHARING_COMMANDS, ACCOUNT_SAFETY_COMMANDS, and the check_destructive filter exemption—from `\bgog\s+` to `\bgog\b.*?\b` to match commands regardless of flags between `gog` and the actual subcommand.

While fixing the patterns, the developer also added a new sheets allowlist feature that blocks writes to non-approved spreadsheets. This required creating an ALLOWED_SHEET_IDS set, implementing a check_sheets_allowlist function that extracts and validates the spreadsheet ID from commands, and integrating it into the main validation chain. The function includes helpful error messaging that tells users how to approve additional sheets if needed.

The developer updated the file documentation to clarify what the hook blocks and added inline comments explaining the allowlist purpose. The changes close a real security gap where migrated commands using the account flag were bypassing all safety restrictions.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- scripts/gogcli-safety-hook.py

**Lines Changed**: ~64 lines
**Message**: "fix(gogcli-hook): match gog commands with -a/--account flag"

═══════════════════════════════════════
## 7:21:44 AM GMT+1 - Commit: 7b20e14

### Summary
The developer completed the M4 rollout by installing native git hooks across 25 repositories plus the Journal, then restructured documentation to reflect the new system architecture.

The work involved three main pieces. First, git hooks were installed batch-wide without approval gates, following Decision 8. The developer verified that branch protection and commit-message hooks fire correctly, and confirmed that commit-story post-commit hooks were preserved across all repos. Second, the documentation was reorganized: hooks-reference.md now leads with the native git hooks section and removed six hooks that had migrated to other systems, while project CLAUDE.md got a new Native Git Hook System section explaining the installation process. Third, several supporting changes were made to narrow scope and clarify triage decisions—the google-mcp-safety-hook.py was refactored to handle YouTube-only operations (moving Calendar, Drive, and Sheets safety to a separate gogcli-safety-hook.py), the CodeRabbit triage rubric in git-workflow.md was updated to distinguish between Fix/Defer/Skip decisions, and an "Asking Multiple Questions" rule was added to the global CLAUDE.md to ensure questions are presented one at a time rather than as a list.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- .claude/CLAUDE.md
- config/settings.json
- global/CLAUDE.md
- rules/git-workflow.md
- rules/hooks-reference.md
- scripts/google-mcp-safety-hook.py

**Lines Changed**: ~163 lines
**Message**: "feat(prd-47): complete M4 rollout — install hooks, update docs"

═══════════════════════════════════════
## 7:32:17 AM GMT+1 - Commit: ea78ef0

### Summary
The developer fixed security and configuration issues identified in a code review. In `lint-changed.sh`, they replaced an unsafe eval statement that concatenated filenames as strings with a mapfile array, eliminating shell injection risk when filenames contain special characters. In `detect-test-tiers.sh`, they passed `PROJECT_DIR` as an environment variable to Python snippets instead of using shell interpolation, which prevents the script from breaking on paths with quotes. They also broadened the Go build tag patterns to match compound expressions like `//go:build linux && integration` instead of just single tags. Finally, they cleaned up `config/settings.json` by removing duplicate hook registrations—the google-mcp and gogcli hooks were registered twice from different paths, so the developer consolidated them to use the correct paths.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- config/settings.json
- hooks/git/lib/detect-test-tiers.sh
- hooks/git/lib/lint-changed.sh

**Lines Changed**: ~32 lines
**Message**: "fix(prd-47): address CodeRabbit findings from M4 review"

═══════════════════════════════════════
## 7:38:29 AM GMT+1 - Commit: 40aaafc

### Summary
The developer simplified Milestone 5 in the native git hook migration PRD, recognizing it had grown into a full project rather than staying focused as a milestone. Instead of trying to evaluate Claude Code plugins and assess their applicability across multiple systems within the existing PRD, the developer decided to spin that work out into its own dedicated PRD for code review plugin evaluation.

The shift came from recognizing a mismatch: M5 wasn't actually about the git hook system anymore—it was about researching plugins and making decisions that would feed into separate future work. The developer restructured it so that M5 now has a single, concrete deliverable: run `/prd-create` to start a new PRD focused on evaluating the Code Review plugin and deciding whether to use it as-is or build a custom skill drawing on its patterns.

The key insight was putting a research spike as the first milestone in that new PRD, so the decision about which approach to take happens before any implementation work begins. The developer also updated the success criteria to reflect this simpler scope—the new PRD just needs to exist and be ready to work on, with that research decision built in from the start.

### Development Dialogue
> **Human:** "This sounds like a full PRD, not a M5 rewrite for something that's unrelated. Also, why would I make my own skill instead of just using the skill that the plugin already exists? I'm thinking we should use /prd-create with a research spike at the beginning To decide whether to use the plug-in or create my own skill And if I am going to create my own skill, use the patterns in the plugin."

> **Human:** "1. Update M5 in the PRD to just be \"create the new PRD\"
2. Run /prd-update-progress to commit the M4 completion
3. Then kick off /prd-create for the code review investigation"

### Technical Decisions
**DECISION: Restructure M5 as a research-first investigation rather than implementation work** (Discussed) - FILES: prds/47-native-git-hook-migration.md

### Commit Details
**Files Changed**:
- prds/47-native-git-hook-migration.md

**Lines Changed**: ~11 lines
**Message**: "docs(prd-47): simplify M5 — create dedicated PRD instead of implementing here"

═══════════════════════════════════════
## 7:42:40 AM GMT+1 - Commit: 0e8f6bc

### Summary
The developer created a product requirements document for evaluating a Code Review plugin as a fallback when CodeRabbit hits rate limits. The PRD outlines a research spike as the first milestone, which gates all downstream decisions—either adopting the existing plugin or building a custom `/review-pr` skill based on what the research uncovers. The document structures the work into clear milestones with specific success criteria, including a research phase that explicitly forbids writing code until findings are documented, followed by two conditional paths for implementation depending on the spike's recommendation.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- prds/65-code-review-plugin-evaluation.md

**Lines Changed**: ~82 lines
**Message**: "docs(prd-65): create PRD for code review plugin evaluation [skip ci]"

═══════════════════════════════════════
## 7:47:15 AM GMT+1 - Commit: b079192

### Summary
The developer archived a completed PRD document for the native git hook migration project. The file `prds/47-native-git-hook-migration.md` was moved to `prds/done/47-native-git-hook-migration.md` with a status header added marking it as complete as of April 9, 2026. This is routine documentation housekeeping—moving a finished project specification into an archive directory and updating its header to reflect completion.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- prds/47-native-git-hook-migration.md
- prds/done/47-native-git-hook-migration.md

**Lines Changed**: ~452 lines
**Message**: "docs(prd-47): mark complete and archive PRD [skip ci]"

═══════════════════════════════════════
## 8:01:50 AM GMT+1 - Commit: 4efdb35

### Summary
The test helper function for the suggest-write-prompt tests was updated to include the `tool_name` field in its input. The hook had been modified to route on `tool_name`, but the test's `make_input` helper wasn't passing that field along, which meant all Write and Edit path tests were silently passing through to exit 0 without generating any advisory output. The fix adds `tool_name` as a parameter with a default value of "Write" and includes it in the JSON structure that gets passed to the hook, bringing the test setup back in line with what the actual hook expects.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- .claude/skills/verify/tests/test_suggest_write_prompt.py

**Lines Changed**: ~4 lines
**Message**: "fix(tests): update suggest-write-prompt tests to include tool_name in input"

═══════════════════════════════════════
## 8:46:45 AM GMT+1 - Commit: 65a695a

### Summary
The developer addressed code review feedback from CodeRabbit on a pull request by fixing security issues, compatibility problems, and linting violations across the git hooks and related scripts.

In the shell scripts, they tightened pattern matching to prevent bypasses: the progress check now uses exact matching (`grep -Fx`) instead of a substring search, and the commit message validator got path-character exclusions to avoid false positives on file paths like `tools/vendor/client.py`. They also fixed a critical security issue in the Python test-tier detection script by passing the project directory through environment variables instead of string interpolation, which prevents injection attacks. The same fix was applied to the JSON output block.

Compatibility was improved in a few places. The `lint-changed.sh` script replaced `mapfile` with a `while-read` loop for Bash 3.2 compatibility on macOS. The git hooks installer now uses `git rev-parse --git-path hooks` instead of hardcoding `.git/hooks`, which properly handles worktrees. The `gogcli-safety-hook.py` script was changed to fail closed when it can't parse the Sheets ID, treating a parse failure as a security risk rather than silently allowing it.

Linting violations were cleaned up: unused variables in `test-tiers.sh` were renamed with underscore prefixes to signal they're intentionally unused, and a variable in `verify-phase.sh` was quoted to fix a ShellCheck warning. The developer also fixed a fallback lint path that was being skipped when no JS or Go files changed, ensuring the fallback command actually runs when provided.

Documentation was updated to fill gaps and remove broken references. The archive README got two missing rows for the pre-commit and pre-push hook files, and the hooks reference was clarified to note that users can bypass native hooks with `--no-verify`. A broken file reference in the microblog API documentation was removed, and markdown formatting issues were fixed by adding blank lines around a table.

The developer deferred three issues to follow up on separately: settings.json path portability, pre-push stdin parsing, and the hardcoded npx in the detect-project script.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- docs/research/michael-forrester-workflow.md
- hooks/archive/claude-code/README.md
- hooks/git/checks/commit-message.sh
- hooks/git/checks/progress-md.sh
- hooks/git/checks/test-tiers.sh
- hooks/git/lib/detect-test-tiers.sh
- hooks/git/lib/lint-changed.sh
- hooks/git/lib/verify-phase.sh
- rules/hooks-reference.md
- rules/microblog-api-gotchas.md
- scripts/gogcli-safety-hook.py
- scripts/install-git-hooks.sh

**Lines Changed**: ~73 lines
**Message**: "fix(prd-47): address CodeRabbit findings from PR #66 review"

═══════════════════════════════════════
## 9:24:53 AM GMT+1 - Commit: 3ae5b13

### Summary
The developer rephrased a git hook message that alerts users when they create issues, shifting it from an instructional tone to a confirmation-style check. Instead of telling users to run `/write-prompt` on an issue, the message now asks whether they already ran it before creating the issue, and if not, to run it now. The developer also simplified the explanation of what counts as a prompt—removing the emphatic "Do NOT skip this" language and replacing it with a more straightforward statement that any issue an AI will read and act on qualifies as a prompt.

### Development Dialogue
> **Human:** "One similar is great as long as you confirmed they're the exact same script. If not, we need to talk about it too. I like your change, except I want to say, \"Did you run the right prompt on this issue body before creating it? If not, run it now.\" Let's talk about those two things, and then we'll cover the rest once those are resolved."
> **Assistant:** "The diff showed one difference — a JSON error handling line that's in claude-config but not in the home dir version. Let me add it so they're identical before symlinking: [...] Files are identical. Creating the symlink: [...] Done. Now the suggest-write-prompt wording. Your phrasing: \"Did you run /write-prompt on this issue body before creating it? If not, run it now.\""

### Technical Decisions
**DECISION: Rephrase issue advisory message from instruction to confirmation** (Implemented) - FILES: .claude/skills/verify/scripts/suggest-write-prompt.sh
  - Hook fires unconditionally after issue creation, but Claude often runs /write-prompt before creating the issue (correct workflow)
  - Current message "Run /write-prompt on this issue body" feels redundant when work was already done
  - Changed to confirmation frame: "Did you run /write-prompt on this issue body before creating it? If not, run it now."
  - Still catches cases where /write-prompt was skipped, but doesn't repeat instructions for already-completed work
  - Clarified that the right prompt must be used, not just any prompt

### Commit Details
**Files Changed**:
- .claude/skills/verify/scripts/suggest-write-prompt.sh

**Lines Changed**: ~8 lines
**Message**: "fix(suggest-write-prompt): rephrase issue advisory as confirmation, not instruction"

═══════════════════════════════════════
## 9:35:39 AM GMT+1 - Commit: 404f6c0

### Summary
The developer fixed issues identified in a code review across four files in the repository's hook and documentation infrastructure.

In the workflow documentation, they removed a machine-specific file path and a note about private access, then simplified a wordy sentence about decision logging.

The main work went into `detect-test-tiers.sh`, a shell script that classifies test files by tier. The developer replaced 11 occurrences of an unreliable `find | head | grep` pattern with `find -print -quit | grep`, which works correctly under `set -o pipefail`. They also reclassified Kubebuilder's envtest as an integration test rather than e2e, updating both the logic and the comment to reflect that distinction.

In `lint-changed.sh`, the developer converted the Go file list from a space-separated string to a bash array. This change improves correctness when file paths contain spaces and makes the handling consistent with how JavaScript files are already processed in the same script.

The documentation file `hooks-reference.md` got restructured for clarity. The developer broke up a dense sentence about the pre-push-verify hook into scannable bullet points, rewrote the introduction to explain git hooks' enforcement model more directly, and split the installation instructions into separate paragraphs so each concern has its own space.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- docs/research/michael-forrester-workflow.md
- hooks/git/lib/detect-test-tiers.sh
- hooks/git/lib/lint-changed.sh
- rules/hooks-reference.md

**Lines Changed**: ~48 lines
**Message**: "fix(prd-47): address second round of CodeRabbit findings"

═══════════════════════════════════════
## 10:05:39 AM GMT+1 - Commit: 5332112

### Summary
The developer updated the prds-get skill and added a deferring rule to the project guidelines. 

The prds-get skill had two versions—a current one and an older v1-yolo variant that was stale. The developer brought them into sync by updating v1-yolo to match the current behavior, which now surfaces both PRDs and standalone issues instead of just PRDs. The changes simplified the fetch process to a single GitHub CLI call and added logic to split results into two groups, mark active PRDs based on the current branch, and analyze dependencies across both PRDs and standalone issues.

The developer also added a new section to CLAUDE.md called "Deferring Known Issues" that establishes when deferral is appropriate and requires explicit user acknowledgment. Rather than silently deferring work, the rule pushes toward fixing things now by default. Deferral only makes sense in narrow cases—when the fix is in a different repo, PRD-scoped, likely to cause merge conflicts, or blocked on investigation that can't happen in the current session. When deferral does apply, the developer surfaces it explicitly and offers three options: fix it now, create a GitHub issue for tracking, or let it go with awareness that it may not resurface. This addresses a broader problem where deferred work was disappearing silently without the developer's knowledge.

### Development Dialogue
> **Human:** "These can be combined. Let's say make the language stronger to discourage deferring. Consider deferral if it's in another repo or if it's PRD-scoped or if it might cause merge conflicts, or whatever else... When a deferral happens, surface this to the user and offer those three options."
> **Assistant:** "I like it a lot. The three-option surface is the key piece that was missing — it turns a silent background decision into an explicit user choice. No more issues quietly appearing in the repo without your awareness. [...] The 'let it go' option is important — it acknowledges that sometimes a finding is genuinely not worth tracking. [...] One question: should this rule live in the CodeRabbit section specifically, or in a more general 'Making Changes' section? I'd lean toward a dedicated section so it applies everywhere (not just CodeRabbit cycles), with a cross-reference in the CodeRabbit section."

> **Human:** "Definitely fix both so that they both have the new functionality. I like the idea of having a deferring known issues section that CodeRabbit references. That seems wise. Good thinking."

### Technical Decisions
**DECISION: Establish explicit deferral protocol with user confirmation** (Implemented) - FILES: .claude/CLAUDE.md
  - Silent deferrals disappear without memory in future sessions
  - Deferral only appropriate for out-of-scope fixes (different repo, PRD-scoped work, merge conflict risk, significant investigation needed)
  - When deferral conditions apply, surface the decision to user before proceeding
  - Offer three explicit options: fix now, create tracked GitHub issue, or let it go with awareness
  - Never proceed with a default choice silently
  Tradeoffs: Adds friction to development flow but prevents issues from disappearing without user awareness

**DECISION: Update prds-get skill to surface both PRDs and standalone issues consistently** (Implemented) - FILES: .claude/skills/prds-get/SKILL.v1-yolo.md
  - v1-yolo variant was stale and only surfaced PRD-labeled issues
  - Updated to match current behavior: fetch all open issues and split into PRDs and standalone issues
  - Present both groups in separate tables for project planning overview
  - Include active branch detection to mark current PRD work
  - Analyze dependencies and blocking relationships across both issue groups
  - Ensures YOLO mode symlinks point to current functionality

### Commit Details
**Files Changed**:
- .claude/CLAUDE.md
- .claude/skills/prds-get/SKILL.v1-yolo.md

**Lines Changed**: ~84 lines
**Message**: "feat: update prds-get v1-yolo to surface standalone issues; add deferring rule to CLAUDE.md [skip ci]"

═══════════════════════════════════════
## 12:33:01 PM GMT+1 - Commit: a03ef99

### Summary
The developer narrowed Bash(rm *) from an allow rule to an ask rule, replacing it with three targeted allow rules for safe temp and log file cleanup patterns.

In the first session, the developer merged work and moved on to planning the next phase. They wanted to tackle some issue juggling and asked what was available without committing to anything yet.

In the second session, the developer was thinking about fixing issues 64, 67, 68, and 69 together in a single branch and commit to reduce code review friction. Before diving in, they wanted to talk through the approach and get a sense of what each issue involved. They asked the AI to look at the issues, then requested a full investigation to understand the scope.

The actual fix addressed issue 64 by moving the broad `Bash(rm *)` pattern out of the allow list and into the ask list, then adding back three specific allow rules: `Bash(rm *.tmp)`, `Bash(rm *.log)`, and `Bash(rm -f *.tmp)`. This keeps common, safe cleanup operations frictionless while preventing accidental deletion of arbitrary files.

### Development Dialogue
> **Human:** "I'm thinking about issue juggling. I want to fix 64, 67, 68, 69 But I kind of want to do them in one branch and one commit to help mitigate code review rate limiting."
> **Assistant:** "The tradeoff is real. Here's how I'd think about it: [...] The rate-limit concern is legitimate but it's an operational inconvenience, not a correctness risk. The one-branch-per-issue rule exists for good reasons (reviewability, rollback, clean history)."

> **Human:** "Look at them."
> **Assistant:** "These fall into two natural groups by file: [...] Bundling each pair makes sense — you'd go from 4 PRs to 2, cutting rate-limit hits in half while keeping each PR coherent and reviewable."

### Technical Decisions
**DECISION: Narrow Bash(rm *) allow rule to safe patterns** (Implemented) - FILES: config/settings.json
  - Broad `Bash(rm *)` pattern creates unnecessary friction for safe cleanup operations
  - Move general `rm *` to ask list, add targeted allow rules for common safe patterns: `*.tmp`, `*.log`, and `-f *.tmp`
  - Preserves frictionless workflow for legitimate temporary and log file cleanup while requiring confirmation for arbitrary deletions
  Tradeoffs: Developers must use specific patterns for rm operations; arbitrary file deletion now requires explicit approval

### Commit Details
**Files Changed**:
- config/settings.json

**Lines Changed**: ~7 lines
**Message**: "fix(settings): narrow Bash(rm *) allow rule to safe patterns"

═══════════════════════════════════════
## 12:40:21 PM GMT+1 - Commit: 3b1ff09

### Summary
The developer fixed two git hook scripts to handle edge cases that were breaking in non-standard repository configurations.

The main change was in the pre-push-verify hook. Previously, it derived the branch name and diff base from the local checkout state and hardcoded 'origin' remote references, which failed when developers were using detached HEAD states, non-origin remotes, or pushing multiple refs at once. The developer rewrote the logic to read the git pre-push stdin payload at the start of the script—the actual data git provides about what's being pushed—and extract the real branch and remote information from there. This handles the edge cases properly while falling back to the old behavior (upstream tracking ref, then origin/main or origin/master) when stdin isn't available. The developer also switched from hardcoding 'origin' to using the REMOTE_NAME variable throughout, so the script respects whatever remote is actually being pushed to.

The second change was in detect-project.sh, which identifies project configuration and generates build/test/lint commands. The problem was that fallback commands for tools like TypeScript, ESLint, Jest, and Vitest were always using 'npx', but that doesn't work with Yarn—especially not with Yarn Berry, which requires 'yarn dlx' instead. The developer added an EXEC variable that gets set based on the detected package manager: 'npx' for npm and pnpm, 'yarn' for classic Yarn, and 'yarn dlx' for Yarn Berry (detected by checking for .yarnrc.yml). Then they replaced all the hardcoded 'npx' calls in fallback commands with this variable.

The developer also added test coverage for both fixes. They wrote a test for the pre-push-verify script that simulates pushing to a non-origin remote (named 'upstream') in a detached HEAD scenario, verifying that the script now correctly derives the diff base from the stdin payload instead of failing. They added two tests for detect-project.sh—one confirming that classic Yarn projects get 'yarn' in their fallback commands, and another confirming that Yarn Berry projects get 'yarn dlx'.

The developer approached this by asking about batching multiple related issues into one branch and commit to reduce code review friction, then investigated the issues before implementing the fixes.

### Development Dialogue
> **Human:** "I'm thinking about issue juggling. I want to fix 64, 67, 68, 69 But I kind of want to do them in one branch and one commit to help mitigate code review rate limiting. What do you think about that? Don't take any action, just talk to me."
> **Assistant:** "The tradeoff is real. Here's how I'd think about it: [...] I'd lean toward following the rule unless the issues are genuinely tiny and the rate limiting has been concretely painful for you recently. What are the four issues? Knowing whether they're related or orthogonal would sharpen the recommendation."

> **Human:** "Look at them."
> **Assistant:** "These fall into two natural groups by file: [...] #68 and #69 are both straightforward shell script fixes with no unknowns — those bundle cleanly. What do you want to do?"

### Technical Decisions
**DECISION: Parse git pre-push stdin payload for accurate ref detection** (Implemented) - FILES: hooks/git/checks/pre-push-verify.sh, tests/git-hook-checks.bats
  - Derive BRANCH and DIFF_BASE from actual refs being pushed rather than local checkout state
  - Handles edge cases: detached HEAD, non-origin remotes, multi-ref pushes
  - Falls back to existing behavior (@{upstream}, origin/main) when stdin is absent
  - Use REMOTE_NAME parameter for REVIEW_BASE instead of hardcoded 'origin'
  Tradeoffs: None explicitly discussed

**DECISION: Map package manager to executor for fallback commands** (Implemented) - FILES: hooks/git/lib/detect-project.sh, tests/git-hook-checks.bats
  - Add EXEC variable derived from detected package manager
  - Yarn classic uses 'yarn', Yarn Berry uses 'yarn dlx', npm/pnpm use 'npx'
  - Apply EXEC to fallback commands (tsc, eslint, jest, vitest) instead of hardcoded npx
  - Detect Yarn Berry presence via .yarnrc.yml file
  Tradeoffs: None explicitly discussed

### Commit Details
**Files Changed**:
- hooks/git/checks/pre-push-verify.sh
- hooks/git/lib/detect-project.sh
- tests/git-hook-checks.bats

**Lines Changed**: ~134 lines
**Message**: "fix(hooks): parse pre-push stdin for accurate ref detection; fix pnpm/yarn executor"

═══════════════════════════════════════
