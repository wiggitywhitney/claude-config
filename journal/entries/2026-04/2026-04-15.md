## 6:03:24 AM CDT - Commit: 6675cc1

### Summary
The developer added a research phase to PRD #58 (workflow session hygiene improvements) to guard against implementing from a stale snapshot of Michael Forrester's workflow repository. The PRD was originally designed from an April snapshot, so the developer introduced a new Step 0 that requires pulling the latest from the workflow repo, reviewing changes since the PRD was written, and documenting findings in a research file that each milestone references before starting.

The developer also applied fixes from a write-prompt review: added Step 0 to the milestone checklist, specified the output structure for the research document (organized by milestone with descriptions of what changed and implementation implications), and updated the Research header link to reference both the original workflow documentation and the update document produced by Step 0.

Additionally, the developer updated `rules/hooks-reference.md` to reflect the current two-tier output format for the `/code-review` plugin, replacing the previous flat-list description with the new structure that separates findings into High confidence (≥80) and Medium confidence (50–79) tiers in a markdown table.

### Development Dialogue
> **Human:** "For PRD 58 coming up, I want to add a step zero where we re-research Michael's repo because it's been a while and he may have updated things. There may be new findings from there that can benefit our system."

> **Human:** "yes change these, don't ask"

### Technical Decisions
**DECISION: Add PRD-level Step 0 to re-research Michael Forrester's workflow repo before starting any milestone** (Implemented) - FILES: prds/58-workflow-session-hygiene.md, PROGRESS.md
  - PRD was designed from a snapshot of Michael's repo taken in early April; he may have added improvements, refactored implementations, or introduced new patterns since then
  - Starting without checking risks implementing an outdated approach
  - Each milestone now references the research doc produced by Step 0, ensuring implementations are based on current patterns rather than stale ones

**DECISION: Update /code-review output format documentation to reflect two-tier confidence tiers** (Implemented) - FILES: rules/hooks-reference.md
  - Code-review plugin now groups findings into High confidence (≥80) and Medium confidence (50–79) tiers instead of filtering below 80
  - Medium-confidence findings now surface as actionable items with Fix/Skip disposition rather than being silently dropped
  - Documentation needed to reflect the new threshold (50 instead of 80) and table-based output format with scores and dispositions

### Commit Details
**Files Changed**:
- PROGRESS.md
- prds/58-workflow-session-hygiene.md
- rules/hooks-reference.md

**Lines Changed**: ~36 lines
**Message**: "docs(prd-58): add pre-implementation research step to session hygiene PRD"

═══════════════════════════════════════
## 7:23:16 AM CDT - Commit: 9f7b547

### Summary
The developer consolidated Michael Forrester's workflow research into a single source of truth by pulling eight new commits from the reference repository and merging all implementation notes directly into the main research document instead of maintaining a separate update file.

The session started with the developer questioning whether keeping research updates in a separate document was the right approach. They asked whether it made sense to incorporate the updates into the original research doc to avoid having two separate files that felt clunky. After confirming this was the right direction, the developer went ahead and merged the implementation notes for all seven milestones inline into `docs/research/michael-forrester-workflow.md` with dated "As of 2026-04-15" entries under each relevant section.

The developer then updated all seven milestone Step 0s in the PRD to reference both the consolidated research document and the specific implementation files to read before writing code. This meant fresh AI instances starting each milestone would have a clear research phase built into their workflow—they'd read the research doc as step zero, understand what changed in the reference implementation since early April, and know exactly which files to examine before implementation began.

The changes also removed the separate update document and updated `PROGRESS.md` to reflect the completion of Step 0. The developer marked the first milestone (config sync script) as next in the queue.

### Development Dialogue
> **Human:** "Should we also incorporate the updates into the original research doc? Instead of having two separate docs that feel a little clunky. What do you recommend and why?"
> **Assistant:** "Merge into the original, but with one structural consideration first. [...] The one thing to preserve: the update doc is organized **by milestone** for easy lookup during implementation. The original is organized by **topic/confidence level**. [...] The cleaner move is to add the implementation notes inline to each relevant section in the original, plus update the Update Log header. [...] This way the doc stays readable top-to-bottom for anyone who hasn't seen it, but also has the current implementation guidance inline exactly where you'd look it up."

> **Human:** "And reference the original in the PRDs and all the PRD milestones if needed. I think it would be good for the fresh AI instance that implements each milestone to have read the research as a step zero of each milestone."
> **Assistant:** "Yes, I completely agree — the research doc provides 'what to adapt'; the actual implementation file provides 'how Michael built it.' A fresh AI needs both before making any design decisions. [...] I'll incorporate that into the PRD edits now."

### Technical Decisions
**DECISION: Consolidate research documentation into a single source of truth** (Implemented) - FILES: docs/research/michael-forrester-workflow.md, prds/58-workflow-session-hygiene.md
  - Merged implementation notes from separate update document inline into the original research doc with dated "As of 2026-04-15:" entries under each affected section
  - Eliminated duplicate documentation by removing the separate update file
  - Updated PRD header to reference only the consolidated research doc instead of two separate documents
  - Preserved the original doc's Update Log table structure to track research refresh dates

**DECISION: Require fresh AI instances to read both research context and reference implementations before starting each milestone** (Implemented) - FILES: prds/58-workflow-session-hygiene.md
  - Updated all seven milestone Step 0 sections to explicitly reference both the research doc (with section pointers for Whitney-specific adaptations) and the corresponding implementation file from the local clone
  - Moved implementation file references from mid-milestone into Step 0 where they serve as required reading before any code work begins
  - Structured Step 0 instructions as numbered list: (1) research doc with section guidance, (2) reference implementation file path, (3) any additional research docs

**DECISION: Document Whitney-specific adaptations inline with reference implementations** (Implemented) - FILES: docs/research/michael-forrester-workflow.md
  - Added implementation notes for config-sync.sh explaining the excludes file pattern and rsync asymmetry (dry-run uses checksum; apply uses size+mtime)
  - Documented post-compact skill and auto-reanchor hook changes: tasks.yaml awareness added in reference but should be skipped for Whitney; replace PROJECT_STATE.md checks with active PRD checks; adapt step 2 to read active PRD instead of PROJECT_STATE.md
  - Noted Stop hook reference doesn't detect bats or vitest; Whitney's implementation needs both as primary test detection cases
  - Documented /continue skill now calls TaskList; Whitney should adopt this using Claude Code's built-in task system instead of YAML
  - Explained /plan-execute skill has no changes; core patterns (state file, re-read before every task, stop on 3 failures, commit after task) remain valid
  - Documented SessionStart hook significantly updated: stdin JSON input pattern, plain text output, .skip-session-resume opt-out dotfile, PROJECT_STATE.md replaced with PRD check; Ralph loop detection already present as planned
  - Noted /cost-tracker reference now uses Observatory CLI that Whitney doesn't have; stick with original bash/jq approach over JSONL files

### Commit Details
**Files Changed**:
- PROGRESS.md
- docs/research/michael-forrester-workflow.md
- prds/58-workflow-session-hygiene.md

**Lines Changed**: ~69 lines
**Message**: "feat(prd-58): complete Step 0 — re-research Michael's workflow repo"

═══════════════════════════════════════
## 8:54:16 AM CDT - Commit: 3a133aa

### Summary
The developer cleaned up global hook configuration by removing unsolicited hooks, project-scoping the EPCAT safety hook to just the Journal repo, and deciding to skip a planned config sync script that was no longer needed.

The session started with the developer questioning whether certain configuration files were actually symlinked to the repo or if they were drifting independently. After investigating the filesystem layout, they discovered that most of the configuration they thought might be drifting—rules files, CLAUDE.md, settings.json, and skills directories—were already symlinked, so the original problem statement for the session hygiene milestone was inaccurate. The real drift surface turned out to be much smaller: just the hooks and scripts directories that lived entirely outside the repo with no symlink back.

The developer identified three specific issues to address. First, there were pre-compact and post-compact hooks that a coworker named Kunal had installed globally without the developer's input—these were easy to remove since the developer didn't want them. Second, there was a global EPCAT safety hook that was running on every Bash call across all repositories, even though only the Journal repo actually uses the EPCAT skill. Third, there were some orphaned scripts cluttering the global scripts directory that weren't referenced anywhere.

For the EPCAT hook specifically, the developer investigated both the Journal version and the advocacy-checkin version to understand the differences, then decided to adopt the advocacy version (which had more advanced payload validation) and scope it to the Journal repo instead of running it globally. This meant creating a project-level settings.json file in the Journal directory to register the hook locally, updating the Journal's skill documentation to keep the advocacy version as the base while preserving the upcoming-talks.md maintenance section as intentional Whitney-specific drift, and removing the global hook registration from the main settings.json.

After this cleanup removed most of the drift surface, the developer reconsidered the first milestone of the session hygiene PRD—a config sync script designed to detect and repair drift between the live `~/.claude/` directory and the repo. With Kunal's hooks deleted, the EPCAT hook project-scoped, and orphaned scripts removed, there was almost nothing left untracked in the global directory. The symlinks and project-scoping approach had solved the problem more directly than a detection-and-repair script would have, so the developer decided to skip that milestone entirely. They updated the PRD to mark M1 as skipped, documented all five decisions made during the session, and changed the PRD status from "Not Started" to "In Progress."

The developer also simplified the global permissions in settings.json by consolidating multiple specific git commands into a single `Bash(git *)` pattern, making the configuration cleaner while still maintaining the safety model through the restricted and dangerous lists.

### Development Dialogue
> **Human:** "Oh, I thought those were symlinked. Will you show me an example of what drifted? Like what literal values drifted?"
> **Assistant:** "Good catch — it's a mixed picture. [...] The entire `rules` directory is a symlink. [...] You're almost fully symlinked already. Michael is not — he has separate copies and wrote the sync script to manage drift between them. So M1 is solving a problem you mostly don't have."

> **Human:** "The EPCAT safety hook could be important. I believe I have that in both my journal repo and the advocacy check-in repo. If they've drifted, they maybe should stay drifted. I don't know. Let's investigate and talk about it."
> **Assistant:** "Here's what I found — three different versions: [...] The drift makes sense. Advocacy is where the real EPCAT data entry happens, so it got the richest validation. Journal only needed basic safety, so it got a cleaner but simpler version. [...] My read: the Journal version is the right shape for the global hook — general Airtable safety, no EPCAT-specific rules. The advocacy version appropriately stays in that repo."

> **Human:** "I want the advocacy hook. Replace the one that's currently in the journal with that. And I suppose scope it to journal instead of having it global. Because only the journal has the EPCAT skill at all, of all my personal repos. How does that sound? but then add back the upcoming-talks.md maintenance - intentional whitney-specific drift"
> **Assistant:** "That sounds clean. [...] Here's what I need to change across four files, plus create one new file and remove the global hook: 1. Replace Journal's hook script with advocacy version 2. Create `Journal/.claude/settings.json` for project-scoped registration 3. Update Journal's `SKILL.md` — advocacy as base + upcoming-talks.md section kept 4. Update `check-prereqs.sh` — change hook check from global to local settings 5. Update `SETUP.md` — Step 1 becomes \"already registered, no setup needed\" 6. Remove global epcat hook from claude-config `settings.json` 7. Delete `~/.claude/scripts/epcat-safety-hook.py`"

### Technical Decisions
**DECISION: Investigate and document actual drift surface in `~/.claude/` configuration** (Discussed)
  - PRD's motivating example (separate edits to `~/.claude/rules/git-workflow.md` and repo copy) was inaccurate — `rules/` directory is symlinked to repo, making that drift impossible
  - Real drift surface is only `~/.claude/hooks/` and `~/.claude/scripts/` for files living outside repo with no symlink back
  - Discovery revealed most configuration already symlinked: `rules/`, `CLAUDE.md`, `settings.json`, `skills/` all point to repo
  - Untracked files found: Kunal's pre-compact/post-compact hooks, EPCAT safety hook, YouTube MCP wrapper, orphaned Google MCP safety hook copy

**DECISION: Remove Kunal's pre-compact and post-compact hooks from global configuration** (Implemented) - FILES: config/settings.json
  - Hooks were installed by coworker without developer's preference
  - Pre-compact hook: `pre-compact-decisions.py`; Post-compact hook: `post-compact-inject.sh`
  - Both removed from `settings.json` registrations and deleted from filesystem
  - Clears the PostCompact hook slot for future M2 implementation

**DECISION: Project-scope EPCAT safety hook from global to Journal repository** (Implemented) - FILES: config/settings.json, Journal/.claude/settings.json, Journal/.claude/skills/epcat/scripts/epcat-safety-hook.py, Journal/.claude/skills/epcat/SKILL.md
  - Global hook was firing on every Bash call in every repository; only Journal uses EPCAT
  - Three versions existed with different maturity levels: global (oldest, debug logging hardcoded), Journal (basic safety), advocacy-checkin (full payload validation with EPCAT-specific rules)
  - Advocacy version selected as implementation: includes mandatory field validation, event name year blocking, city deduplication with diacritical handling
  - Hook moved from `~/.claude/scripts/epcat-safety-hook.py` to `Journal/.claude/settings.json` registration pointing to project-local script
  - Journal's SKILL.md replaced with advocacy version as base, preserving upcoming-talks.md maintenance section and applying `/write-prompt` review recommendations
  - Eliminates global scope drift; self-contained model allows portable setup across team members
  - Tradeoffs: Intentional drift preserved in upcoming-talks.md maintenance (Whitney-specific, not part of advocacy version)

**DECISION: Delete orphaned and dead-code scripts from `~/.claude/scripts/`** (Implemented) - FILES: config/settings.json
  - `google-mcp-safety-hook.py`: orphaned dead copy; actual hook fires from repo path via global settings.json
  - `youtube-mcp-wrapper.sh`: only referenced in completed PRD historical records, not in any active settings
  - Stale permission entry in Journal's `settings.local.json` cleaned up
  - Leaves only active scripts: `gdrive-mcp-wrapper.sh` (Google Calendar MCP, still in use)

**DECISION: Skip M1 (config sync script) — drift surface eliminated by symlinks and project-scoping** (Discussed)
  - After cleanup of Kunal's hooks, EPCAT hook project-scoping, and orphaned scripts, virtually no untracked files remain in `~/.claude/`
  - Symlink-based approach (already in place for rules, CLAUDE.md, settings.json, skills) solved drift problem more directly than detection-and-repair script would
  - Problem M1 was designed to address no longer exists at meaningful scale
  - M1 removed from active milestones; M2–M7 remain independent and unaffected

### Commit Details
**Files Changed**:
- PROGRESS.md
- config/settings.json
- prds/58-workflow-session-hygiene.md

**Lines Changed**: ~60 lines
**Message**: "feat(prd-58): clean up global hook config and skip M1"

═══════════════════════════════════════
