## 6:37:21 AM CDT - Commit: 706c60d

### Summary
The developer added a new watch-item to the writing voice rules after noticing that "worth" can read as an AI tell when used in value-judgment phrasing like "that's context worth adding." They documented this during a review of a draft talk on agent traces, flagging it as something to watch for rather than an outright ban. The entry includes both the problematic pattern and a rephrasing approach—stating things directly instead of wrapping them in a value judgment. The developer committed this separately since it was a voice rule captured during other work, not part of the surrounding audit milestone.

### Development Dialogue
> **Human:** "My recommendation: split them by what's in them. design-decisions → claude-config, because it's generic tooling with nothing private and it's useful in every repo. podcast-review-loop → claude-personal, because of the personal email, the Drive folder ID, and the client repo name.

I accept."
> **Assistant:** "Executing the split. First, recon on `claude-personal` — I need its structure and branching rules before touching it."

> **Human:** "keep going"
> **Assistant:** "All three suites in: **bats 378/14** (identical to the documented pre-existing groups — no regression), **verify 466/466**, **test_setup.py 156/156**. That's **98.6% passed (1000/1014), 1.4% failed (14/1014), 0 skipped** — the 14 being the two known groups."

### Technical Decisions
**DECISION: Document "worth" as a potential AI tell in writing voice rules** (Discussed) - FILES: rules/writing-voice.md
  - Flagged during review of agent-traces talk draft as reading like generated output in value-judgment phrasing
  - Recorded as a watch-item rather than an outright ban, with explicit rephrasing guidance
  - Direct statements preferred over noun-form value judgments ("Your platform team should add that context" instead of "That's context worth adding")

### Commit Details
**Files Changed**:
- rules/writing-voice.md

**Lines Changed**: ~5 lines
**Message**: "docs(rules): capture the "worth" tell in the writing voice rules"

═══════════════════════════════════════
## 6:37:42 AM CDT - Commit: 0a3faa6

### Summary
The setup script had been broken in two independent ways, both of which prevented machines from provisioning correctly. The developer fixed both issues and repaired a test suite that had been silently crashing.

The first problem was that `setup.sh` carried a hardcoded list of six skills to install while the repository actually holds twenty-four. This meant a freshly provisioned machine would come up missing every skill needed for the PRD and issue workflows—the ones used daily. The developer changed both the install and uninstall paths to derive their lists from the actual directory structure instead of maintaining a list that could drift out of sync. They verified the fix by confirming that reverting to the old hardcoded approach would fail the new tests and expose all eighteen missing skills.

The second problem was that `setup.sh` was exiting with an error on every run and installing nothing at all. The script validates that each hook path in the settings template actually exists, but the template still pointed at `scripts/google-mcp-safety-hook.py`—a YouTube MCP safety hook that had been deleted earlier in the same milestone. That single deletion ended up recorded in four different places: the live settings (cleaned when it was removed), the settings template (stale), a test fixture (stale), and a test assertion (stale). Only the live settings had been cleaned up. The fourth location—the test assertion—should have caught this immediately, but the test suite was crashing earlier in its run, so it never got that far.

The crash itself turned out to be the most important finding. The test suite was aborting at test three of one hundred fifty-six with an uncaught error, which meant the run produced no failure lines at all. The CI comparison table was recording this as zero failures, which looked like reassurance for twelve days—but zero failures and "the suite never got past test three" are the same reading unless you check whether a total was printed. The developer annotated the table to flag this.

The developer also replaced a hardcoded expected hook-path count with one derived from the settings template, since a literal number there breaks every time a hook is added or removed and makes failures look like "someone changed the hooks" rather than "a hook path is broken." They added tests asserting that every skill and command in the repository gets provisioned, and they caught a subtle issue where two new test functions had been appended after the `if __name__ == "__main__"` block, which meant they were never actually called—they existed, were correct, and did nothing. The tests moved above the block and now run.

After these changes, the test suite went from aborting after two tests to running all one hundred fifty-six tests, all passing. Across the full test suite, there are now one thousand passing tests and fourteen failing (the same pre-existing failures that were there before).

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- .claude/commands/design-decisions.md
- PROGRESS.md
- docs/research/claude-config-repo-audit.md
- journal/summaries/daily/2026-08-18.md
- journal/summaries/daily/2026-08-19.md
- settings.template.json
- setup.sh
- tests/test_setup.py

**Lines Changed**: ~288 lines
**Message**: "fix(prd-109): repair setup.sh, which installed a quarter of the skills and had stopped running"

═══════════════════════════════════════
## 6:40:02 AM CDT - Commit: e78fe43

### Summary
The developer measured document sizes across the project and documented findings about growth patterns and dead material that should be removed. The PRD and decision log had each roughly doubled in size since the original separation rule was established, violating the principle that was meant to keep them in check. Rather than simply restating the rule, the developer noted that the growth included valuable corrections—each one necessary to prevent future errors—creating a genuine tension between readability and accuracy. The developer recommended reconsidering the measurement approach: tracking growth rate or applying caps selectively rather than a single byte threshold across all documents.

The developer also conducted a sweep for unused files and directories. An empty `profiles/` directory created in February was never populated and should be removed. The `hooks/archive/claude-code/` directory containing 7 files and 38 KB of code was superseded by work in a closed PRD and referenced only in historical records; git history preserves it better than a stale copy. Two anki image scripts have no references outside the journal entry from the day they were written and can be removed unless run manually. The migration script for this PRD should be removed when the PRD closes, flagged now to prevent it from becoming permanent through inattention. The developer kept other scripts and templates with explicit reasons documented—each one either invoked by active systems, covered by tests, or documented in setup instructions.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- docs/research/claude-config-repo-audit.md

**Lines Changed**: ~52 lines
**Message**: "docs(prd-109): measure document sizes and sweep for dead material"

═══════════════════════════════════════
## 5:10:19 PM CDT - Commit: ed6590e

### Summary
The developer updated three documentation files to track two previously unrecorded issues that the audit had flagged in early August.

The work split a set of fourteen failing tests into two separate issues based on their nature rather than lumping them together. Ten of the failures reproduce consistently on clean machines—a genuine bug with an unknown cause—so that became issue #115, framed as a bounded debugging task. The other four failures are environment-dependent; a trial run on a hosted machine revealed an additional forty-three tests that only pass locally because the machine has network access, credentials, and specific repositories in place. Those findings, plus the absent CI that blocks on them, became issue #116.

The developer updated PROGRESS.md to document the split and why it mattered—combining them would bury the fixable half inside an open-ended design problem. They also updated the audit document to note that both groups are now tracked as of the commit date, with links to the new issues. In the PRD itself, they corrected the wording that had said "nothing tracks them" and added context about the reproduce command and the reasoning behind the split, including how the acceptance criteria for #115 initially created a problematic dependency that the developer then resolved by showing how to recover the deleted CI workflow onto a branch.

### Development Dialogue
> **Human:** "two issues"
> **Assistant:** "Both drafted. Running `/write-prompt` on them before creating, per the issue-creation rule."

> **Human:** "what is the podcast-review-loop backup?

I need more context for those three other questions too, but ask me just one at a time."
> **Assistant:** "[...] `podcast-review-loop` is a skill you wrote on 2026-07-13. It lives at `~/.claude/skills/podcast-review-loop/SKILL.md`, 3,817 bytes, and it's reachable in every session as `/podcast-review-loop`. [...] The reason it matters more than its size suggests is the process knowledge baked into it — the just-in-time review model where only one podcast's issue is open at a time so each rebuild reads the current style spec, that issues #4–#17 were closed as superseded and must not be reopened, and that the rules-compliance rebuild has to be the first ask before your own review. None of that is recoverable from anywhere else. [...] It was committed to neither config repo, and `backup-private-files.sh` doesn't look at `~/.claude/` at all — so it existed on this laptop and nowhere else. [...] I didn't push because running that repo's own `sync-push.sh` would have swept about four months of un-backed-up personal memory files into the same commit, and pushing is outward-facing enough that I wanted your say. **Push the branch and open a PR, push straight to `main`, or leave it local for you to handle?**"

> **Human:** "oh i see. I need it for ai inevitable repo only

push and open a PR"
> **Assistant:** "Got it. Pushing and opening the PR."

### Technical Decisions
**DECISION: Split failing tests into two separate tracking issues based on root cause and scope** (Implemented) - FILES: PROGRESS.md, docs/research/claude-config-repo-audit.md, prds/109-claude-config-audit-redesign.md

### Commit Details
**Files Changed**:
- PROGRESS.md
- docs/research/claude-config-repo-audit.md
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~6 lines
**Message**: "docs(prd-109): track the failing tests and absent CI in two issues"

═══════════════════════════════════════
