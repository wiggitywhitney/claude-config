## 9:30:57 AM CDT - Commit: 7ccf764

### Summary
The developer restructured the progress-recording skill to make pushing a normal part of the workflow instead of something to actively avoid.

The issue came to light when the developer discovered the repository had accumulated 104 unpushed commits. The old instruction explicitly told whoever ran the skill not to push, framing a commit as a local checkpoint that preserved progress. That phrasing sounded reassuring but wasn't — work trapped on a single laptop is one disk failure away from disappearing, and the repository had quietly built up nineteen days and forty-four commits of it, including twenty-two journal files that the project's rules identify as unrecoverable. Nothing in the workflow made that gap visible, so nobody noticed it growing.

The developer rewrote both the careful and autonomous variants of the skill to push after the automated CodeRabbit review runs, not before. This timing matters: findings get triaged while the work is still local, so problems can be fixed immediately rather than discovered later when pushing becomes harder. The push waits for findings to be triaged (fixed, deferred to a tracked issue, or skipped with a stated reason) rather than waiting for a clean report, because the review rules allow deliberately skipping a finding — waiting for zero findings would mean never pushing at all.

The developer also added explicit guidance for what to do when a push gets refused. The pre-push hooks or review gates might deny it for reasons like a missing changelog entry or a failed security check. When that happens, the instruction is to do what the refusal asks and try again, never bypass the checks with flags like `--no-verify`, since those checks are the only things preventing secrets or unreviewed code from reaching the remote.

The changes went into the skill documentation files and the progress log, with the step lists renumbered to reflect the new push step in its proper place in the sequence.

### Development Dialogue
> **Human:** "in claude-config, we have 104 commits that we haven't pushed?"
> **Assistant:** "[...] The branch **is** pushed, tracking `origin/feature/prd-109-claude-config-audit-redesign`. My \"104 unpushed\" was commits ahead of `main`, which is just the branch's length, not unpushed work. I used the wrong base and repeated the number several times. The real figure is **44 commits ahead of the remote branch**. And the gate agrees — its base resolves to `@{upstream}`, not `origin/main`, so the outgoing diff it would demand a review of is those 44 commits, not the whole branch."

> **Human:** "Have you not been pushing and running coderabbit on each /prd-update-progress? Does coderabbit have an upper limit for how much it will review?"
> **Assistant:** "[...] **CodeRabbit: yes, twice today.** But note what it was reviewing — the documented command is `--base origin/main`, so each run reviewed the **entire branch**, not just new commits. That's why both rounds returned findings on journals from early August I never touched. **Pushing: no, deliberately.** `/prd-update-progress` ends with \"Do NOT push commits unless explicitly requested by the user.\" So the 19-day gap is the documented behavior working as written — which is arguably the real finding here, since the skill's rule and your journal-preservation rule pull against each other and nobody noticed until the count got large."

> **Human:** "If CodeRabbit's been reviewing, then that's fine. Go ahead and push. I don't know why you have an instruction not to push."
> **Assistant:** "[...] **the no-push instruction isn't something I invented.** It's in two places you own: `~/.claude/CLAUDE.md`, under Git: *\"Commit or push only when the user asks.\"* [and] `prd-update-progress/SKILL.md`, Step 8: *\"Do NOT push commits unless explicitly requested by the user. Commits preserve local progress checkpoints without affecting remote branches.\"* [...] But the reasoning in that skill note is what didn't survive contact: \"commits preserve local progress checkpoints\" is true of the commit, and it quietly implies the work is safe. It isn't — a commit that never leaves the laptop is one disk failure from gone, and this branch had 19 days and 22 journal files sitting in exactly that state."

> **Human:** "If we want to push, just write the push as part of the PRD update progress skill."

### Technical Decisions
**DECISION: Reposition push operation to occur after CodeRabbit review instead of before** (Implemented) - FILES: .claude/skills/prd-update-progress/SKILL.md, .claude/skills/prd-update-progress/SKILL.v1-yolo.md

**DECISION: Make pushing a standard part of progress recording workflow rather than optional** (Implemented) - FILES: .claude/skills/prd-update-progress/SKILL.md, .claude/skills/prd-update-progress/SKILL.v1-yolo.md, PROGRESS.md

**DECISION: Change push readiness condition from "clean review" to "triaged findings"** (Implemented) - FILES: .claude/skills/prd-update-progress/SKILL.md, .claude/skills/prd-update-progress/SKILL.v1-yolo.md

**DECISION: Add explicit handling for push refusal scenarios** (Implemented) - FILES: .claude/skills/prd-update-progress/SKILL.md, .claude/skills/prd-update-progress/SKILL.v1-yolo.md

**DECISION: Document pre-push hook timeout behavior and verification procedure** (Implemented) - FILES: .claude/skills/prd-update-progress/SKILL.md, .claude/skills/prd-update-progress/SKILL.v1-yolo.md

**DECISION: Clarify security implications of bypassing pre-push verification** (Implemented) - FILES: .claude/skills/prd-update-progress/SKILL.md, .claude/skills/prd-update-progress/SKILL.v1-yolo.md

### Commit Details
**Files Changed**:
- .claude/skills/prd-update-progress/SKILL.md
- .claude/skills/prd-update-progress/SKILL.v1-yolo.md
- PROGRESS.md

**Lines Changed**: ~68 lines
**Message**: "feat: make pushing part of recording progress, instead of something to avoid"

═══════════════════════════════════════
