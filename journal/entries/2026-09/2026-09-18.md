## 7:06:05 AM CDT - Commit: 1184fe7

### Summary
The developer updated the global prompt guidelines to require that every question presented to them include the pros and cons of each option alongside a stated recommendation.

This came from observing a pattern where presenting bare choices forced the developer to do the analysis work themselves, then ask for it as a follow-up step. By folding the analysis into the initial question, the AI could give more useful guidance upfront. The developer added an exemption for trivial confirmations where the tradeoff is self-evident, keeping the rule practical and avoiding boilerplate.

The changes went into two places: the main prompt file (`global/CLAUDE.md`) got a new section under "Presenting Options" that spells out the requirement, and the progress log documented the decision with context about why it matters. The developer also noted in the progress log that this rule applies whether there's one question or several, and emphasized that the analysis should be part of asking, not a follow-up.

### Development Dialogue
> **Human:** "What are the pros and cons of each, and what do you recommend? Every time you present me with a question, give me the pros and cons of each option and a recommendation. Put that in global CLAUDE.md."
> **Assistant:** "[...] Every question you present — whether one question or several — must include, for each option, the pros and cons and a stated recommendation. Skip this for trivial confirmations where the tradeoff is genuinely obvious. Never present a bare choice and wait for the user to weigh tradeoffs unassisted."

### Technical Decisions
**DECISION: Require pros, cons, and recommendation with every question presented to the user** (Discussed) - FILES: global/CLAUDE.md, PROGRESS.md

### Commit Details
**Files Changed**:
- PROGRESS.md
- global/CLAUDE.md

**Lines Changed**: ~6 lines
**Message**: "docs(global): require pros, cons, and a recommendation with every question"

═══════════════════════════════════════
## 10:03:54 AM CDT - Commit: d0ca114

### Summary
The developer evaluated the operating-system-level command sandbox by actually enabling it and running commands inside it, rather than reading about it in documentation. The sandbox works, and two claims recorded in earlier research are now corrected: the tool tells the assistant which network hosts a command may reach instead of interrupting the user for approval each time a new one comes up, and a one-line exemption fixes the one real casualty—a family of command-line tools that verify web certificates through a macOS service the sandbox blocks by default. Two traps are worth knowing: the affected tool reports the failure as an invalid stored password, so anyone hitting it would re-authenticate to fix something that was never broken, and a helper agent reading only that output drew exactly that wrong conclusion until a before-measurement caught it; and switching the sandbox on does not by itself create a boundary, since two of the three settings that make it enforceable default the other way.

The developer also found that a personal copy of the code-review command, made in April from the official version, takes precedence over the one built into the tool—which is documented behavior, not a bug, but means the newer built-in features were unreachable. Asking for the cloud-hosted review tier had been silently discarding the request for months. Three copies of the same reviewer turn out to be installed at once, and the one that wins is the oldest. The April copy does keep one thing the built-in lacks: when a review finding is real but too big to fix on the spot, it opens a tracking issue, where the built-in simply marks it "not applied" and records it nowhere. So the copies get consolidated later rather than deleted now.

The developer closed the reviewer thread by finding that Viktor's own configuration carries a commented-out all-Claude reviewer alternative, meaning the same-vendor configuration was tried and deliberately switched off in favor of having a different vendor's model check the work. This settled the question of whether to run `ultrareview`—it will not be adopted and was never run, since Viktor's lead is unusually legible: not an absence of evidence about the same-vendor path, but a rejected alternative sitting in his live config.

Findings were integrated directly into the existing research documents rather than creating a new survey, per the milestone's success criteria. The sandbox evaluation was added to the permission-modes research document, the reviewer findings to the subagent-capabilities document, and the progress block in the PRD was updated to reflect what remains open for the next milestone.

### Development Dialogue
> **Human:** "So Viktor uses sandboxing?"
> **Assistant:** "[...] No — and Milestone B2 recorded that explicitly rather than by omission. [...] So on the isolation question, both reference practitioners came back negative."

> **Human:** "What does Viktor do? I trust him and would follow his lead rather than take the time to do this."
> **Assistant:** "[...] Viktor doesn't use Claude's reviewer at all — and that's the answer to your question, though not the one that saves time. [...] His six roles, and the models behind them [...] Each role has a **commented-out all-Claude alternative**, so he set up the same-vendor version, looked at it, and deliberately chose against it. That's the strongest signal available on the question I was about to spend your money on: Viktor's lead says don't make a Claude reviewer your main check."

### Technical Decisions
**DECISION: Evaluate the command sandbox empirically rather than from documentation** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - Milestone A3 deferred sandbox adoption pending evaluation; B1 was tasked to run it rather than survey it
  - Enabled sandbox in scratch settings file without modifying live configuration
  - Captured unsandboxed baseline before testing to distinguish new failures from pre-existing ones
  - Verified filesystem confinement by disk state (file absence) rather than reported denial messages
  - Verified network egress filtering with domain-specific blocking and violation notices
  Tradeoffs: Sandbox evaluation required two-week-earlier answer would have been wrong due to 2.1.275 fix for hooks/ and config/ directory writes; this repo has both directories

**DECISION: Correct two claims in permission-modes research based on empirical sandbox testing** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - Per-domain prompt class objection no longer holds at 2.1.276; blocked domains fail with violation notice to model, not prompt to user
  - Per-command allowed_domains mechanism (added 2.1.271) routes host approval through classifier alongside command
  - Go-CLI breakage confirmed with concrete error (x509: OSStatus -26276) traced to Seatbelt profile denying trustd mach-lookup
  - One-line excludedCommands exemption verified working as remedy
  - Marked superseded sections in place with forward pointers rather than deleting them

**DECISION: Document adoption trap that enabling sandbox alone does not create a boundary** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - allowUnsandboxedCommands defaults to true, leaving dangerouslyDisableSandbox parameter honored
  - failIfUnavailable defaults to false, so sandbox that cannot start runs commands unconfined
  - Two of three settings that make sandbox enforceable default the other way
  - OS-enforcement advantage is conditional on explicit hardening configuration

**DECISION: Identify that gh reports invalid credential when token is actually valid under sandbox** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - Sandboxed gh auth status reports "The token in keychain is invalid" when unsandboxed baseline shows token logged in and working
  - User hitting this message would reasonably re-authenticate or log out to fix nothing
  - Sub-agent reading only sandboxed output drew wrong conclusion without baseline
  - Symptom points away from cause; only baseline distinguished new failure from pre-existing one

**DECISION: Decline ultrareview adoption based on Viktor's rejected all-Claude alternative** (Discussed) - FILES: docs/research/claude-code-subagent-capabilities.md
  - Viktor's six-role configuration contains commented-out all-Claude reviewer alternative
  - Same-vendor configuration was written, considered, and deliberately left disabled
  - ultrareview is Claude reviewing Claude, same vendor as author
  - Reviewer trial's central finding was that same-vendor reviewer shares author's blind spots
  - CodeRabbit already provides mixed-vendor checking; ultrareview does not replace it
  - Never run; no cost figure established; no evaluation performed

**DECISION: Record that personal code-review skill shadows bundled skill by documented precedence** (Discussed) - FILES: docs/research/claude-code-subagent-capabilities.md
  - April copy of code-review skill symlinked from repo takes precedence over bundled /code-review
  - /code-review ultra silently discards effort argument rather than reaching cloud tier
  - Bundled alias /review still reaches built-in skill; aliases are not shadowed
  - Documentation explicitly states personal skill replaces bundled command but not its aliases
  - Three implementations installed simultaneously; April fork (stalest) wins by precedence

**DECISION: Identify that fork's Defer disposition is genuinely absent from bundled skill** (Discussed) - FILES: docs/research/claude-code-subagent-capabilities.md
  - Fork creates GitHub issue via gh issue create when finding is real but too big to fix immediately
  - Bundled skill's only post-fix outcomes are fixed, no_change_needed, skipped
  - Skipped outcome means "real but not applied" and tracks nowhere
  - Bundled skill produces exactly the silent deferral that project's own CLAUDE.md warns against
  - Consolidation cannot simply delete fork without replacing Defer capability

**DECISION: Hand reviewer consolidation to Milestone C1 rather than implement during research phase** (Discussed) - FILES: docs/research/claude-code-subagent-capabilities.md
  - B1 is research milestone; PRD flags reviewer trial as "the one place this PRD implements inside Phase B"
  - Renaming fork changes what /code-review means in five live references across rules and skills
  - Consolidation is C1's stated job for skill and hook pruning
  - /code-review keeps working unchanged in meantime; /review reaches bundled skill today
  - Maintains phase discipline by not implementing during research

**DECISION: Record Viktor's two free reviewer practices as C1 inputs** (Discussed) - FILES: docs/research/claude-code-subagent-capabilities.md
  - Review and audit as two separate read-only roles dispatched in parallel
  - Resolution filter is agreement-or-disagree rather than severity; shipping past finding requires documented reason
  - Both practices are prompt-level only; natively available in Claude Code today
  - No new infrastructure required

**DECISION: Document method notes for sandbox evaluation to enable reuse** (Implemented) - FILES: docs/research/claude-code-permission-modes.md, prds/109-claude-config-audit-redesign.md
  - strings over shipped binary at ~/.local/share/claude/versions/<version> yields highest-yield instrument
  - Binary carries full settings schemas with describe() text, generated Seatbelt policy, bundled skill prompts
  - --settings <file> layers additional settings without touching live configuration
  - --allowedTools is variadic and swallows trailing prompt; pass probes on stdin instead
  - Capture unsandboxed baseline before enabling anything to distinguish new failures
  - Verify filesystem claims by disk state not reported messages
  - -p transcript often preserves only final assistant message; ask for single short output or verify side effect

**DECISION: Record open items for Milestone B1 with cold-start entry points** (Discussed) - FILES: prds/109-claude-config-audit-redesign.md
  - Project-level @-import compaction probe; 10,937 bytes of C1's byte budget depends on answer
  - Milestone B4's token-cap question; whether authoritative count obtainable with different tokenizer
  - Milestone B3's pre-landing-gate question; whether deterministic gate exists natively
  - Milestone B2's hook-event question; whether event fires on every user prompt
  - autoMode.environment still unconfigured; unattended run survival question untouched
  - Baseline capability sweep largely untouched; CLI surface enumerated incidentally

### Commit Details
**Files Changed**:
- PROGRESS.md
- docs/research/claude-code-permission-modes.md
- docs/research/claude-code-subagent-capabilities.md
- journal/summaries/daily/2026-09-11.md
- journal/summaries/weekly/2026-W37.md
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~381 lines
**Message**: "docs(prd-109): evaluate the command sandbox and close the reviewer thread"

═══════════════════════════════════════
## 10:10:33 AM CDT - Commit: 1be8835

### Summary
The developer updated documentation to correct an overstated claim about a command-shadowing issue. The changelog and research document had described the built-in code-review feature as unreachable when a personal copy took precedence, but investigation showed the feature remained accessible through its shorter alias—only the full command name resolved to the older April version. The developer narrowed both documents to describe the actual collision: the full name ran the personal copy and silently discarded cloud-tier requests, while the built-in stayed available under its alias, making this a naming conflict rather than a lost capability.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- PROGRESS.md
- docs/research/claude-code-subagent-capabilities.md

**Lines Changed**: ~4 lines
**Message**: "docs(prd-109): scope the reviewer-shadowing claim to the colliding name"

═══════════════════════════════════════
## 10:11:51 AM CDT - Commit: 450fcdf

### Summary
The developer updated the project documentation to record two unresolved questions about the sandbox mechanism that emerged during evaluation. The first issue is a conflict between two independent sessions conducted the same morning: one documented the sandbox escape hatch as an automatic retry outside the sandbox when commands are blocked, while the shipped configuration schema describes it as a parameter the model may pass. Since these readings imply different threat models and neither was tested, the developer flagged the question as open and marked it for resolution before sandbox configuration work proceeds. The second loose end is a duplicate rule file (`rules/claude-code-sandbox-gotchas.md`) that needs reconciliation with the main findings rather than being left to diverge, which the developer noted should happen alongside other rule-file consolidation work planned for later.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- PROGRESS.md
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~5 lines
**Message**: "docs(prd-109): record two unresolved loose ends from the sandbox pass"

═══════════════════════════════════════
## 10:21:08 AM CDT - Commit: 3f7faf8

### Summary
The developer updated the PRD document to record three decisions from the Milestone B1 evaluation session and cascaded them into Milestone C1's inherited work.

Decision 91 documents the sandbox evaluation: the sandboxed Bash tool was tested on 2.1.276 and found viable, retiring the objection from Milestone A3. The evaluation confirmed filesystem confinement and network filtering both work, identified the concrete error blocking `gh`/`gcloud`/`terraform` (an x509 certificate verification failure), and verified the one-line `excludedCommands` fix. The decision notes that enabling the sandbox requires three settings to be configured together, and flags two unsettled claims about the escape-hatch behavior and Docker compatibility for Milestone C1 to resolve.

Decision 92 declines the Claude-on-Claude reviewer tier (`ultrareview` and cloud multi-agent review), recording it as a deliberate choice rather than leaving it as unfinished work. The decision cites Viktor's existing disabled all-Claude alternatives in the config as stronger evidence than running a new test, and notes that the mixed-vendor property is the whole point of a code reviewer—a property Claude Code cannot supply.

Decision 93 defers the consolidation of three installed `code-review` implementations to Milestone C1 instead of handling it in Milestone B1. The decision identifies that the personal fork, though stalest, cannot simply be deleted because its `Defer` disposition opens tracking issues while the bundled skill's equivalent outcome (`skipped`) records findings nowhere. The consolidation requires replacing rather than absorbing the `Defer` behavior, and renaming the fork would affect five workflow references across multiple files—work deferred on phase-discipline grounds since Milestone B1 is a research milestone.

The numbering (91, 92, 93) was chosen after a collision with existing decision IDs (83–85 were already taken), which the commit message notes was caught only after rows were inserted.

### Development Dialogue
> **Human:** "run /prd-update-decisions then /prd-update-progress then do handoff check"

No significant dialogue found for this development session.

The human's message is a single-line command directive with no explanatory context, reasoning, or substantive dialogue. The assistant's responses that follow are technical execution logs and status updates—not conversational exchanges with the human that would yield meaningful quotes about the developer's thinking or decisions.

The development work itself is fully documented in the commit message and code diff, which show the three decisions (91, 92, 93) being recorded and propagated to Milestone C1. Those artifacts are the primary sources for understanding what was accomplished.

### Technical Decisions
**DECISION: Sandbox adoption as viable candidate for Milestone C1** (Discussed) - FILES: prds/109-claude-config-audit-redesign.md
  - Evaluated by enabling in scratch settings and running commands, not by re-reading documentation
  - Filesystem confinement and network egress filtering verified by disk state and proxy response
  - Milestone A3's objection about network domain prompts is retired; per-command allowed domains now route approval through classifier
  - `gh`/`gcloud`/`terraform` breakage confirmed as `x509: OSStatus -26276` and fixed by single `excludedCommands` entry
  - Adoption requires all three settings configured together; defaults weaken the boundary
  - Two unsettled questions remain: escape hatch mechanism (automatic retry vs. model parameter) and whether `docker` under Colima is Unix-socket issue
  - Proposed configuration shape and capability labels recorded in permission-modes research

**DECISION: Claude-on-Claude reviewer declined** (Discussed) - FILES: prds/109-claude-config-audit-redesign.md
  - Same-vendor arrangement (Anthropic Claude reviewing Claude code) matches Viktor's disabled configuration
  - Viktor's commented-out all-Claude alternatives in live config provide stronger evidence than new evaluation
  - Mixed-vendor property is essential to checker function; Claude Code cannot provide vendor switch for sub-agents
  - CodeRabbit already supplies mixed-vendor capability
  - Decision recorded to prevent later misinterpretation of absence of cost figure as unfinished work

**DECISION: Code-review consolidation deferred to Milestone C1** (Discussed) - FILES: prds/109-claude-config-audit-redesign.md
  - Three implementations installed simultaneously: April personal fork, enabled `code-review@claude-plugins-official` plugin, bundled skill
  - Personal fork shadows bundled skill by documented precedence; `/code-review ultra` discards effort argument while `/review` reaches bundled skill
  - Fork is not redundant: its `Defer` disposition opens tracking issue for real-but-too-large finding; bundled skill's `skipped` outcome records finding nowhere (silent deferral)
  - Consolidation must replace `Defer` rather than absorb it
  - Rename affects five workflow references in `rules/git-workflow.md`, `rules/hooks-reference.md`, and `issue-done`, `prd-done`, `issue-create` skills
  - Deferred on phase-discipline grounds: Milestone B1 is research phase; reviewer sub-agent trial is flagged as exception to Phase B implementation rule
  - `/code-review` continues working unchanged during deferral

**DECISION: Three decisions propagated to Milestone C1 scope** (Discussed) - FILES: prds/109-claude-config-audit-redesign.md
  - Skill consolidation now owns code-review collision with both constraints documented
  - Permission model's conditional sandbox decision converted to live decision with configuration constraints and two closing questions
  - No cross-PRD propagation; PRD #84's tracked copy marked stale and resolved by later milestone

### Commit Details
**Files Changed**:
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~6 lines
**Message**: "docs(prd-109): log the sandbox, reviewer, and consolidation decisions"

═══════════════════════════════════════
## 10:29:04 AM CDT - Commit: 0b954d0

### Summary
The developer corrected documentation across four files to distinguish between configuration evidence and run evidence, addressing two cases where claims reached further than their sources supported.

In the first correction, the developer clarified Viktor's approach to same-vendor code review. The original documentation stated he "tried" an all-Claude reviewer configuration and "switched it off," but the actual record shows only that each of his reviewer roles carries a commented-out all-Claude alternative—a standing preference visible in his live config, not a measured verdict from running the variant. The developer updated the decision log, research notes, progress log, and PRD to reflect this distinction precisely, making clear that configuration evidence (the disabled alternative in his setup) differs from run evidence (actually executing and rejecting the variant).

In the second correction, the developer pruned the sandbox configuration's `excludedCommands` list from three entries down to one. The original list included `gh *`, `docker *`, and `colima *`, but only `gh` had been tested—observed failing inside the sandbox and working once exempted. The `docker *` entry was untestable because the required daemon wasn't running, and `colima *` was pure inference. Since every entry in that list punches a hole in the sandbox boundary, the developer kept only the verified entry and redirected attention to `allowUnixSockets` as the narrower approach to evaluate for Colima, moving the untested entries to the open questions section.

The developer also added a procedural note to the decision log explaining how to add rows without collisions: compute the table's maximum entry number rather than reading the last rows, since entries have been inserted mid-table and the numbering is no longer monotonic with file order.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- PROGRESS.md
- docs/research/claude-code-permission-modes.md
- docs/research/claude-code-subagent-capabilities.md
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~26 lines
**Message**: "docs(prd-109): separate configuration evidence from run evidence"

═══════════════════════════════════════
## 10:44:42 AM CDT - Commit: 505caed

### Summary
The documentation was updated to distinguish between tested findings and inferences in the TLS verification research. The developer corrected claims about `gcloud` and `terraform` tool failures by marking them as mechanism-based inference rather than confirmed observations, since only `gh` was actually run in the sandbox test. The same pattern of overstated claims was identified as recurring, prompting a broader fix: the decision log's warning about table numbering was rewritten to avoid naming a specific row number, which became stale as soon as new rows were added. Additionally, the sandbox configuration section was expanded to require testing of scoped `allowMachLookup` permissions before approving any tool exemptions, elevating this from a footnote to a prerequisite decision.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- docs/research/claude-code-permission-modes.md
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~6 lines
**Message**: "docs(prd-109): confine the TLS finding to the tool that was actually run"

═══════════════════════════════════════
## 12:05:57 PM CDT - Commit: 3eb51d0

### Summary
The developer committed four new gotcha files documenting practical pitfalls across four tools: web-tree-sitter, devbox, dot-agent-deck, and Claude Code's sandbox. Each file was scoped to load only when relevant files are present in the working tree, and all four were indexed in the rules README. The developer also fixed two existing rule files with corrections to the code-review tool's behavior and updated the scoring documentation to catch a hostname leak that had shipped in earlier evaluation traces.

The work was stranded across several earlier sessions and committed to main rather than a feature branch because rule files load from the working tree — committing them to a branch would make them disappear on main until that branch merged, defeating their purpose. The developer noted this explicitly in the commit message and confirmed the docs-only exemption applied.

The new files documented real problems: grammar packages ship native addons that require fetching the WebAssembly file separately from GitHub Releases; devbox lacks a Homebrew install path despite appearing to have one, and its `init_hook` doesn't get sourced as documented; dot-agent-deck requires an explicit `agent` key per role or status tracking silently stops working; and Claude Code's sandbox has several defaults that weaken it, including failing open when it can't start, plus the observation that devbox provides no process isolation at all despite being commonly assumed to.

The developer also captured two ways the automated code-review tool confidently reports wrong findings: it misreported a correctly committed binary as untracked with a hash matching nothing on disk, and it flagged an external API field as unstable based on an unrelated badge elsewhere in the same documentation file rather than checking the field's own entry. The developer added guidance to check primary sources before accepting such findings.

In the scoring rules, the developer caught and documented a hostname leak where 31 lines of evaluation traces shipped the real machine name because `host.name` and `host.id` are separate fields — redacting one doesn't redact the other. The developer also documented a partial-path checkout cleanup gotcha: restoring a repo after checking out only `src/` or `examples/` from an instrumented branch leaves behind companion `.instrumentation.md` files that `git checkout` never deletes, and using `git clean -fd` to remove them is unsafe because it deletes any untracked file under those paths, not just the known artifacts.

The Claude Code sandbox file was committed as written despite disagreeing with a parallel research pass about whether the unsandboxed escape hatch is an automatic retry or a parameter the model passes — that question is tracked separately, and editing the file to match the other account would erase independent documentation rather than resolve the disagreement.

### Development Dialogue
> **Human:** "So Viktor uses sandboxing?"
> **Assistant:** "[...] No — and Milestone B2 recorded that explicitly rather than by omission. [...] So on the isolation question, both reference practitioners came back negative."

> **Human:** "What does Viktor do? I trust him and would follow his lead rather than take the time to do this."
> **Assistant:** "[...] His setup needs `dot-agent-deck` (a brew-installed Rust daemon and TUI), three non-Claude CLI harnesses, GPT API access for all three, and a 220-line TOML orchestration config. [...] Your instinct to not spend time here is right, and there's a principled reason beyond that — B1's job is to hand C1 a list of collapse candidates, and this is now a well-evidenced one."

> **Human:** "should we commit and own those changes? Wrap them up with this branch? I'm inclined to do that. wdyt?"
> **Assistant:** "[...] Good instinct on not leaving them dangling, but let me check two things first — whether a session is still live in those files, and whether they'd pass the repo's own gates. [...] Recommendation: Option 1. Your instinct to stop leaving it stranded is right; I'd just land it on `main` rather than here, because that's the only option where the rules are both safe and in effect."

### Technical Decisions
**DECISION: Sandbox adoption as a permission-model candidate** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - Empirically verified on local install (v2.1.276, macOS Seatbelt): filesystem confinement blocks writes outside working directory, network egress filtering denies unlisted domains with 403 response
  - Per-command `allowed_domains` mechanism (added 2.1.271) eliminates the "every new domain prompts" objection from prior research; blocked domains now fail with model-directed notice rather than user prompt
  - `gh` TLS breakage confirmed with exact error (`x509: OSStatus -26276`), traced to Seatbelt profile denying `com.apple.trustd.agent` Mach lookup required by Go's TLS verification
  - `excludedCommands: ["gh *"]` remedy verified working
  - Adoption requires three settings together: `sandbox.enabled: true`, `sandbox.failIfUnavailable: true`, `sandbox.allowUnsandboxedCommands: false`; defaults leave escape hatches
  - 2.1.275 fix for sandboxed writes to `hooks/` and `config/` directories verified by disk state (relevant: this repo has both)
  Tradeoffs: Sandbox is unprecedented in both reference practitioners' setups (Viktor's config has commented-out all-Claude alternative, deliberately disabled); adopting it means discovering rough edges without outside evidence of production durability

**DECISION: Decline `ultrareview` cloud multi-agent code review** (Discussed)
  - `ultrareview` is Claude reviewing Claude (cloud tier of built-in `/code-review` skill), same-vendor as author
  - Reference practitioner (Viktor) deliberately configured mixed-vendor checking (three GPT harnesses) and left same-vendor alternative disabled; rejected alternative visible in live config outranks constructed test
  - Reviewer trial's central finding: checker sharing author's model shares author's blind spots; CodeRabbit (mixed-vendor) already fills that role
  - No cost incurred; decision made on configuration evidence rather than empirical evaluation

**DECISION: Defer `code-review` skill consolidation to Milestone C1** (Discussed) - FILES: docs/research/claude-code-subagent-capabilities.md
  - Three installed reviewers exist: personal fork (April, symlinked from repo, shadows `/code-review`), official plugin (enabled, unreached), bundled built-in (reachable via `/review`)
  - Personal fork's `Defer` disposition (creates GitHub issue via `gh issue create`, runs `/write-prompt`, links issue back to PR) is unique; bundled skill's `skipped` outcome tracks nothing, which is the silent-deferral failure the project's own CLAUDE.md warns against
  - Bundled skill's verify pass (CONFIRMED/PLAUSIBLE/REFUTED per candidate) is more rigorous than fork's 0–100 confidence scoring
  - Consolidation requires reconciling three implementations and updating five workflow references; deferred to C1 (skill consolidation milestone) on phase-discipline grounds (B1 is research, not implementation)

**DECISION: Record method gotchas from sandbox evaluation** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - Variadic `--allowedTools` flag swallows following arguments; use stdin for prompt when testing with multiple flags
  - Extracting strings from native binary (`strings` over minified bundles) yields highest-signal surface enumeration
  - `--settings` loads additional settings without touching live config; enables safe isolated probes
  - Baseline-before-change required; sandboxed failure is only attributable to sandbox if unsandboxed baseline shows same operation succeeds
  - Filesystem claims verified by disk state, not reported output
  - `-p` flag on `jq` keeps only final message; prevents log truncation from hiding earlier context

**DECISION: Record three unsettled claims from sandbox evaluation** (Discussed) - FILES: docs/research/claude-code-permission-modes.md
  - Docker under Colima: Colima not running on this machine; Unix socket wrinkle undocumented in official docs (assumes Docker Desktop)
  - `allowMachLookup` as narrower alternative to `enableWeakerNetworkIsolation`: not tested; named as open question
  - Changelog interval 2.1.222–2.1.264: fetch truncated; unknown whether sandbox-relevant changes exist in gap

**DECISION: Record `gh` error message misleads under sandbox** (Implemented) - FILES: docs/research/claude-code-permission-modes.md
  - Under sandbox, `gh auth status` reports "The token in keyring is invalid" when token is valid and works unsandboxed
  - Unsandboxed baseline essential to distinguish sandbox restriction from actual credential failure; without it, user would reasonably re-authenticate or log out to fix non-existent problem

**DECISION: Record CodeRabbit CLI misreads binary files and external specs** (Implemented) - FILES: rules/git-workflow.md
  - CLI reported correctly committed binary (`resources/tree-sitter-python.wasm`) as untracked with SHA-256 matching neither committed blob nor working-tree file; likely sandbox/binary-diff-extraction artifact in review tool
  - CLI flagged external API attribute as "Development" status based on unrelated inline badge elsewhere in same source file, when attribute's own registry entry stated `stability: stable`
  - Before accepting CodeRabbit finding about binary file's tracked state/hash or external spec's documented properties, independently verify with `git ls-files`, `git show HEAD:<path> | shasum`, and primary-source documentation

**DECISION: Record CodeRabbit CLI first-run billing flag requirement** (Implemented) - FILES: rules/git-workflow.md
  - First invocation with `--agent` may emit `action_required` event (`"action":"rerun_with_use_credits"`) and exit with "Error: Rate limit exceeded" instead of running review
  - Not the transient rate-limit case (no wait time given, recurs every invocation without `--use-credits`)
  - Fix: add `--use-credits` to command; confirmed run then proceeds through setup/analyzing/reviewing phases to complete event with `usageCost` summary
  - Check log for `action_required` before treating bare "Rate limit exceeded" as transient case

**DECISION: Record hostname field leaking in evaluation traces** (Implemented) - FILES: rules/is-scoring-gotchas.md
  - Redaction list included `host.id` but not `host.name`; they are separate fields, so redacting one does not redact the other
  - Confirmed on commit-story-v2 run-28 (2026-09-18): `eval-traces-run28.json` shipped real hostname on all 31 lines while `host.id` was correctly redacted
  - Sanitization checklist must include both fields

**DECISION: Record unsafe cleanup after partial-path branch checkout** (Implemented) - FILES: rules/is-scoring-gotchas.md
  - `git checkout <branch> -- src/ examples/` never deletes paths; instrument branches add `.instrumentation.md` companion files left behind as staged or untracked
  - Full restore: `git reset HEAD -- src/ examples/ && git checkout -- src/ examples/ && find src/ examples/ -name '*.instrumentation.md' -delete`
  - Do not use `git clean -fd src/ examples/` for deletion step; removes any untracked file under those paths, not just known `.instrumentation.md` artifacts, risking deletion of unrelated work-in-progress files
  - Verified with `git status --short` after cleanup

**DECISION: Record web-tree-sitter grammar package installation gotchas** (Implemented) - FILES: rules/web-tree-sitter-gotchas.md
  - Grammar npm packages (e.g. `tree-sitter-python`) ship both `.wasm` file and native prebuilds; `npm install` runs `node-gyp-build` install script regardless, which uses matching prebuild or falls back to native source build
  - Get `.wasm` from grammar repo's GitHub Releases page instead of npm to avoid install-script side effects and native build-tool dependencies
  - `await Parser.init()` must be called before creating any `Parser` instance
  - `Language.load()` loads `.wasm`; `Language.loadSync()` for precompiled `WebAssembly.Module` (Cloudflare Workers, Vercel Edge only)
  - Parser ABI must match grammar ABI; `web-tree-sitter` 0.24.x supports ABI 13–14, >= 0.25.0

### Commit Details
**Files Changed**:
- PROGRESS.md
- rules/README.md
- rules/claude-code-sandbox-gotchas.md
- rules/devbox-gotchas.md
- rules/dot-agent-deck-gotchas.md
- rules/git-workflow.md
- rules/is-scoring-gotchas.md
- rules/web-tree-sitter-gotchas.md

**Lines Changed**: ~122 lines
**Message**: "docs(rules): land four new gotcha files and three review-tool corrections"

═══════════════════════════════════════
## 12:12:13 PM CDT - Commit: 54b20a0

### Summary
# Session Summary

The developer merged the `main` branch into a feature branch, resolving a conflict in `PROGRESS.md` by incorporating recent updates. The merge surfaced and documented several tool-related findings that had been captured separately during development work.

The commit itself is a standard merge operation with conflict resolution. The substantive changes—detailed documentation of tool gotchas, sandbox evaluation findings, and reviewer-related discoveries—were already present in the codebase and are being integrated into the main development line through this merge. No new implementation work occurred in this specific commit; rather, it represents the integration of research and documentation that had been completed on the feature branch.

### Development Dialogue
No significant dialogue found for this development session

### Technical Decisions
No significant technical decisions documented for this development session

### Commit Details
**Files Changed**:
- PROGRESS.md
- rules/README.md
- rules/claude-code-sandbox-gotchas.md
- rules/devbox-gotchas.md
- rules/dot-agent-deck-gotchas.md
- rules/git-workflow.md
- rules/is-scoring-gotchas.md
- rules/web-tree-sitter-gotchas.md
- PROGRESS.md
- docs/research/claude-code-permission-modes.md
- docs/research/claude-code-subagent-capabilities.md
- journal/summaries/daily/2026-09-11.md
- journal/summaries/weekly/2026-W37.md
- prds/109-claude-config-audit-redesign.md

**Lines Changed**: ~518 lines
**Message**: "Merge branch 'main' into feature/prd-109-milestone-b1-capability-spike"

═══════════════════════════════════════
## 5:59:35 PM CDT - Commit: 1998931

### Summary
The developer turned off background feedback drafting in the configuration and recorded the journal entries that had accumulated during the session's work. The `feedbackDrafts` setting was switched to "off" in the settings file, and a progress note was added documenting this change. The commit also preserved the journal entries that the commit hook had appended, preventing them from being left uncommitted in the working tree.

### Development Dialogue
> **Human:** "Go ahead and commit and wrap into the work we're doing on this branch."

### Technical Decisions
**DECISION: Disable background feedback drafting in configuration** (Implemented) - FILES: config/settings.json, PROGRESS.md
  - Preference was already in effect locally and needed to be recorded in tracked configuration
  - Prevents the assistant from queuing feedback reports during a session
  - Aligns configuration file with actual session behavior

**DECISION: Commit journal entries appended by commit hook** (Implemented) - FILES: PROGRESS.md
  - Journal entries were appended by the commit hook during the session and needed to be landed rather than left stranded in the working tree
  - Ensures session work is properly recorded in version control

**DECISION: Preserve model key at tracked value rather than session-specific value** (Discussed)
  - The model key field tracks whichever model is selected at the time and is not a change worth recording
  - Session happened to use a different model than the tracked value, but this is not a preference change to commit

### Commit Details
**Files Changed**:
- PROGRESS.md
- config/settings.json

**Lines Changed**: ~3 lines
**Message**: "chore(config): turn off background feedback drafting, and land the journal"

═══════════════════════════════════════
## 6:30:52 PM CDT - Commit: 2d1efae

### Summary
The developer incorporated findings from parallel work in other repositories into the project's rule documentation. A new file, `ruff-black-gotchas.md`, was created to capture practical differences between the Ruff and Black Python formatters—specifically that Black exits with a non-zero code but still outputs the original source on parse failure, while Ruff outputs nothing, making exit code the reliable way to detect success rather than checking for non-empty stdout. The developer also tightened the cleanup procedure for partially checking out branches by capturing the exact set of added paths before resetting, then deleting only those paths rather than using pattern-based deletion that could accidentally remove unrelated files. Three writing-voice rules were added to `writing-voice.md`, each drawn from messages as actually sent: using "I have some [topic] news!" as an opener instead of flat "good news" phrasing, qualifying unfamiliar third parties as "someone named [Name]" rather than assuming the reader knows them, and using "telemetry" instead of "payload" when addressing a non-implementation audience. The developer also documented that evaluation trace files are JSON Lines format despite their `.json` extension, so review tools flagging them as invalid JSON are encountering an expected false positive, and reformatting them into a single document breaks the scorer. These changes were recorded in `PROGRESS.md` with the new rule added to the index in `rules/README.md`.

### Development Dialogue
> **Human:** "Another repo made some changes in here. Will you please incorporate those changes into this branch and everything? Just own them."

### Technical Decisions
**DECISION: Document Python formatter failure behavior differences** (Discussed) - FILES: `rules/ruff-black-gotchas.md`, `rules/README.md`
  - Black exits with code 123 on parse failure but still prints the original unformatted source to stdout
  - Ruff exits with code 2 and produces empty stdout on parse failure
  - Detecting success by checking "stdout is non-empty" silently passes unformatted code through when using Black
  - Exit code should be the branch point for success detection, not stdout presence
  - `ruff format -` stdin support is real but undocumented in help output
  - `ruff check --fix` does not support stdin→stdout round-trip like `ruff format` does
  - Python's `-c` syntax check traceback has two `File` lines; the real line number is on the second one, not the first

**DECISION: Tighten repository cleanup after partial branch checkout** (Discussed) - FILES: `rules/is-scoring-gotchas.md`
  - Previous pattern-based deletion could catch same-named files that predated the checkout
  - New approach captures exact paths the instrument branch added before reset, then deletes only those
  - Using `git diff --diff-filter=A` to identify added paths is more precise than filename pattern matching
  - `git clean -fd` removes any untracked file under the paths, not just known artifacts
  - Pattern-based `find -name '*.instrumentation.md' -delete` is still broader than necessary

**DECISION: Document JSON Lines format of evaluation trace files** (Discussed) - FILES: `rules/is-scoring-gotchas.md`
  - Evaluation trace files are one JSON object per line despite `.json` extension
  - Scorer reads them with `content.trim().split('\n')` and `JSON.parse()` per line
  - Converting to single top-level JSON document or renaming extension breaks the scorer
  - Code review tools flagging them as invalid JSON is expected and not a defect

**DECISION: Adopt three writing-voice rules from actual message usage** (Discussed) - FILES: `rules/writing-voice.md`
  - Lead updates with "I have some [topic] news!" rather than flat "Good news on [topic]" for more personal, excited tone
  - Qualify unfamiliar third parties as "someone named [Name]" instead of assuming reader knows who they are
  - Use "telemetry" instead of "payload" when describing tool output to non-implementation audiences

### Commit Details
**Files Changed**:
- PROGRESS.md
- rules/README.md
- rules/is-scoring-gotchas.md
- rules/ruff-black-gotchas.md
- rules/writing-voice.md

**Lines Changed**: ~51 lines
**Message**: "docs(rules): adopt the gotchas produced while working in other repos"

═══════════════════════════════════════
