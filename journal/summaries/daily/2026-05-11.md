# Daily Summary — 2026-05-11

## Narrative

The developer spent the day documenting and consolidating work from other agents, then addressing review feedback. The session began with a large commit capturing twelve daily journal entries and two weekly summaries spanning April 8–25, 2026, documenting a two-week period of infrastructure development. That work covered moving Claude's global settings into version control, building a native git hook migration system, integrating code review plugins, designing session management tools like the `/continue` skill, and creating autonomous PRD execution using a Ralph loop architecture. The developer also built a `/cost-tracker` skill for token spend reporting.

Following that, they committed a substantial documentation update capturing gotcha rules, writing guidelines, and skill threshold changes across the codebase. This included raising Anki card quality standards from 9/15 to 12/15, expanding the global CLAUDE.md reference with Docker/Colima setup and journal preservation rules, and creating five new gotcha files covering TypeScript CLI surprises, LinkedIn API quirks, IS scoring setup, mermaid-cli gotchas, and GitHub CLI fork behavior. Existing gotcha documentation for Kyverno, Weaver, and Micro.blog was expanded significantly—Kyverno grew from 3 sections to 8, Weaver from 2 to 9, and Micro.blog gained 3 new sections. The developer also created an entirely new writing-voice.md file extracting Whitney's actual communication patterns from real emails and Slack messages, capturing rules about subject usage, greeting conventions, closing style, and avoiding em dashes. They tightened the `/code-review` skip criteria to include a foreground-only constraint ensuring the tool runs as a blocking call rather than in parallel. The developer discussed this work with the AI, confirming they needed one CodeRabbit review across the entire diff rather than commit-level reviews, and noting that all changes were docs-only so `/code-review` could be skipped per git workflow rules.

After that, they updated PROGRESS.md to record the accumulated changes from the agent-changes-catchup branch, then addressed code review findings with a small commit correcting a spelling error ("implementor" to "implementer"), clarifying that the GitHub fork gotcha list was illustrative, adding a cross-reference between Mermaid documentation and Quarto settings, and fixing a markdown formatting issue in a journal entry.

Finally, the developer corrected a git command reference in the branch protection documentation, changing `HEAD..<branch>` to `main..<branch>` to accurately reflect the proper syntax for checking journal commits before branch deletion, and added an inline clarification that journal markdown files are exempt from the feature-branch requirement.

## Key Decisions

- **Raise Anki card quality threshold from 9/15 to 12/15** — Cards must now reach 12 points to pass without rewrite, strengthening the quality gate for spaced repetition effectiveness.

- **Establish automatic writing-voice rule extraction from Whitney's corrections** — When Whitney edits a draft or shares what she actually sent, extract the principle and update writing-voice.md immediately. If her real sent version contradicts a rule, remove the rule. This captures actual communication patterns rather than prescriptive guidelines.

- **Implement journal file preservation rules across all branches** — Never delete or lose journal files. Before deleting a branch, check for unmerged journal commits and cherry-pick them to main. Never use force delete on branches with unmerged journal commits.

- **Document Colima Docker runtime setup** — Whitney uses Colima instead of Docker Desktop. Docker CLI and Docker Compose work identically; only the runtime changed. Check colima status and start if needed before running Docker commands.

- **Add acceptance gate failure triage process** — Complete a 6-step triage whenever an acceptance gate run fails before merging. Every active failure must live in the open backlog with full description of what needs fixing.

- **Tighten /code-review skip criteria** — Skip for docs-only PRs, standalone issue fixes with ≤2 non-test source files changed with new tests and no blocking CodeRabbit findings, or other small/obvious changes. Never run in the background or in parallel.

- **Add Kyverno policy validation webhook timeout fix** — Use kinds: ["*"] (wildcard) in exclude blocks to bypass named-kind validation when cluster has unavailable API groups.

- **Document Kyverno API client rate limit tuning** — Raise both admission and background controllers to 100/100 QPS/burst at install time for CRD-heavy environments to prevent leadership lease loss.

- **Implement webhook-level API exclusion via matchConditions** — Use CEL expressions in matchConditions (K8s 1.27+) to exclude apiextensions.k8s.io from webhook rules, preventing the call from being made entirely.

- **Document Kyverno CRD discovery limitation** — Kyverno only discovers API resources at startup; CRDs registered after startup are invisible. Restart the admission controller after new CRDs are registered.

- **Add GKE Konnectivity deadlock prevention** — Install Kyverno with forceFailurePolicyIgnore enabled to use failurePolicy: Ignore on all webhooks, preventing TokenReview denials during restart.

- **Document Micro.blog XML-RPC API differences** — getPages blogID must be the username string, not integer 1. Response uses id (not pageID) and <i4> (not <int>) for integers. About page has is_template: false.

- **Add progressive reveal technique for Mermaid diagrams in Quarto** — Put the full diagram on every slide and hide unrevealed elements by color-matching their background. Never add or remove nodes between slides.

- **Document Weaver subprocess HOME propagation requirement** — Always pass HOME explicitly in env options when spawning weaver as a child process.

- **Document Weaver 0.21.2 and 0.22.1 output format differences** — 0.21.2 returns single JSON object; 0.22.1 streams individual entity objects then writes statistics last. /stop endpoint returns "OK"; read stdout for actual compliance data.

- **Document Weaver binary PATH and launcher compatibility** — Weaver binary installed to ~/.cargo/bin/weaver. When running under launchers that strip PATH, construct path using os.homedir() and add ~/.cargo/bin to process.env.PATH.

- **Create writing-voice.md with Whitney's communication style rules** — Captures patterns like dropping subjects, using "Hello" vs "Hey" based on relationship, closing with "Warmly," on its own line, cutting unnecessary backstory, using "notified" instead of "looped in," and avoiding em dashes.

## Open Threads

No open threads identified.
