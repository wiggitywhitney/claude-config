# Daily Summary — 2026-05-31

## Narrative

The developer built a new pre-commit advisory hook today that checks prompt generality without blocking commits. When `src/agent/prompt.ts` gets staged, the hook fires three diagnostic questions to stderr: whether the guidance applies broadly (not tied to the eval target), whether it addresses root causes rather than symptoms, and whether examples use synthetic namespaces instead of real ones. The hook always exits successfully, so it never stops a commit—it's purely informational.

The developer used test-driven development, writing nine failing tests first to specify the exact behavior, then implementing the bash logic to make them pass. The tests cover the key cases: staying silent when nothing relevant is staged, printing the advisory with all three questions when the prompt file is detected, and verifying the exit code is always 0. They also check edge cases like similarly named files that shouldn't trigger the check, ensuring the path matching is tight.

The new script, `check-prompt-generality.sh`, was integrated into the existing pre-commit dispatcher alongside the branch protection and progress tracking checks. It fits cleanly into the workflow without disrupting anything. The developer then updated PROGRESS.md to document the work.

## Key Decisions

No key decisions documented today.

## Open Threads

No open threads identified.
