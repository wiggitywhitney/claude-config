# Daily Summary — 2026-07-06

## Narrative

The developer started the day by reviewing a large batch of documentation changes across the branch—four new gotcha rule files for pino, sharp, OTel Logs Bridge API, and Datadog log-trace correlation, plus extensions to five existing gotcha files documenting verified findings from recent integration work. They also reformatted the gotcha index in `CLAUDE.md` as a nested reference-pointer list to reduce token cost, added a new acceptance-criteria gating rule to the git workflow, clarified that the `/code-review` skill is PR-only, enabled the trajectory plugin marketplace, switched the default model to opus, and committed journal entries documenting prior work on autonomous PRD execution and the GitHub issue workflow skill suite. This was a routine documentation commit preparing the branch for standard workflow processing.

After that batch landed, the developer added three journal summaries capturing the month of June work—daily, weekly, and monthly entries that documented the broader context of automation infrastructure, commit discipline improvements, and the complete six-skill GitHub issue workflow suite.

Then CodeRabbit review findings came back on PR #107, and the developer spent the rest of the morning addressing inaccuracies across the documentation. In `is-scoring-gotchas.md`, they tightened guidance about verifying that port 4318 is actually held by `otelcol-contrib` before reusing it, since other processes could be holding it. In `pino-gotchas.md`, they reclassified `import-in-the-middle` from a devDependency to a runtime dependency because production installs commonly prune devDependencies, and the `--import` bootstrap flag needs this package available at startup. In `sharp-gotchas.md`, they corrected how prebuilt binaries are actually distributed—as separate `@img/sharp-*` scoped optional dependencies, not via postinstall script—and explained that globally installed libvips can trigger a source build that often fails. They also improved writing voice guidance by reworded "guest invite prospects" to "prospective guests" for clarity, and extended reference-pointer wording in `global/CLAUDE.md` to standardize two remaining gotcha-file links that were still in the old `@-reference` style.

Once those corrections were committed, the developer noticed that `writing-voice.md` was bloating the context-loading cost because it's auto-loaded into every conversation via the reference in `global/CLAUDE.md`. They trimmed it by moving the "Slide Speaker Notes Voice" section to `presentation-slides.md` (which is only read when actually writing speaker notes) and removing two worked examples that just restated principles already captured as compact rules. This cut roughly 30% of the file's context-loading overhead while preserving all distinct guidance. They then added a progress log entry documenting that trim.

## Key Decisions

- **Reformat gotcha-file index in CLAUDE.md as nested list with explicit reference-pointer note** — Convert flat `@` reference list to nested bullet structure with a note clarifying these are manual lookups, not auto-loaded context, to reduce token cost and prevent unnecessary reading of all gotcha files.

- **Create four new gotcha rule files for emerging technologies** — Document verified findings from recent integration work (pino, sharp, OTel Logs Bridge API, Datadog log-trace correlation) before knowledge is lost.

- **Extend existing gotcha files with additional verified findings** — Add findings to IS scoring, Kyverno, Micro.blog API, OTel semantic conventions, and OTel Span Metrics Connector gotcha files.

- **Add Closes-#N acceptance-criteria gating rule to git-workflow.md** — Before linking an issue in a PR description with `Closes #N`, every unchecked acceptance criteria checkbox must be either implemented in the current branch or tracked in a new open issue to prevent silent deferral of acceptance criteria.

- **Add /code-review PR-only clarification to issue-create skill** — Clarify that `/code-review` requires an open PR and is only for PRs, not GitHub issues.

- **Enable trajectory plugin marketplace and switch default model to opus** — Enable the trajectory plugin from local marketplace and switch default model from sonnet to opus for improved reasoning on complex tasks.

- **Verify otelcol-contrib process identity before reusing port 4318 listener** — Port 4318 may be held by unrelated or stale processes; must check that `otelcol-contrib` is specifically the listening process before skipping startup, or resolve the conflict.

- **Classify import-in-the-middle as runtime dependency, not devDependency** — The `--import` bootstrap flag requires this package at production startup, and production installs commonly prune devDependencies, so explicit runtime declaration ensures it survives.

- **Correct sharp prebuilt binary distribution model** — Prebuilt binaries are distributed as separate `@img/sharp-*` scoped optional dependencies; globally installed libvips can trigger a source build that often fails, preventable with `SHARP_IGNORE_GLOBAL_LIBVIPS=1`.

- **Standardize reference-pointer wording for gotcha-file links** — Extend reference-pointer annotations across remaining gotcha-file links in `global/CLAUDE.md` to clarify these are manual lookups, not auto-loaded context.

- **Relocate Speaker Notes Voice rules to presentation-slides.md** — Move situational speaker-notes guidance from `writing-voice.md` (auto-loaded into every conversation) to `presentation-slides.md` (read only when writing speaker notes) to eliminate unnecessary context-loading cost.

- **Remove redundant worked examples from writing-voice.md** — Two full annotated examples duplicate lessons already stated as compact rules; removing them cuts ~30% of the file's context-loading overhead while preserving all distinct guidance.

## Open Threads

No open threads identified.
