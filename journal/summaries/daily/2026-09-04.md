# Daily Summary — 2026-09-04

## Narrative

The developer spent the day working through a detailed audit of skill-family divergence and consolidation planning. They started by establishing hard measurements on code differences between upstream and local skill variants using two new committed scripts. The fork-point script revealed that the initial assumption was backwards — the upstream skills had only moved 20 lines over seven months, while local copies had diverged by 419 lines. This finding let them reprioritize the work: instead of a full line-by-line comparison, they'd give the upstream's 20 lines thorough treatment, summarize the local's 419 by section, and spend the freed effort on six upstream skills that don't exist locally.

They deep-read those six upstream skills and labeled each by adoption readiness. Two of them (`verify-pr` and `reproduce-first`) were ready to adopt immediately since they had no dependencies. Three queue-selection skills and one autonomy-suppression skill needed swarm integration before adoption. The six swarm roles revealed that enforcement was prompting-only rather than structural, so the transferable parts were the test-plan-first gate and the two-gates-only rule.

While documenting divergence between the interactive and YOLO variants, they hit a practical problem: the `prd-done` skill had exceeded its 5,000-token compaction cap in both variants. The human mentioned they'd stopped using the Anki capture step, and suggested extracting it into a separate skill. The developer created `/make-anki-cards` with a sourcing strategy that deduplicates against existing cards before reading source material, then removed the Anki section from both `prd-done` variants. They also replaced five instances of duplicated rules with pointers to the rules file, which got both variants safely under the cap with real margin. While editing, they fixed two live correctness issues: a backreference that pointed to commands that had moved, and an instruction to squash commits that contradicted the repo's own git rules.

An inertness audit confirmed all 128 project-level lifecycle symlinks across 16 repos are shadowed by personally-installed skills, so they're all inert. This also surfaced that the consumer list was incomplete — the PRD named nine YOLO repos, but seven additional repos symlink the interactive variants, bringing the total to 16.

They documented five findings that contradicted what the PRD currently states: the consumer count is 16 repos, not 9; Decision 76a doesn't generalize past eight PRD skills; a seventh upstream skill appeared in a recent refresh; the known-cases list about CodeRabbit's three-channel fetch is stale; and `prd-done` exceeded the compaction cap (which they fixed rather than deferred). They also enumerated extraction candidates by grepping for repeated procedures — the CodeRabbit family spans three implementations and is the clearest case, while PROGRESS.md creation appears twelve times and hasn't drifted yet, making it the cheapest to consolidate before it does.

After that, they updated the PRD document to record the nine decisions from the skill-family diff and cascade them into affected milestones. This included expanding the consumer set, clarifying byte-identity scope, documenting the seventh upstream skill, closing the Anki divergence, fixing the size defect, and identifying three research documents that still describe a retired mechanism. Progress reached 54% complete.

They created a journal entry documenting Decision 77 on transcript extraction for the D2 milestone — formalizing that a committed, re-runnable script should handle enumeration and redaction deterministically while a model classifies human turns. The reasoning was that completeness claims need mechanization, and manual redaction of transcripts with credentials poses both reproducibility and security risks.

Then they handed off an unresolved tokenizer measurement question to the capability spike milestone. The completion skill's size fix left the audit sitting within the margin where different tokenizers disagree — they used `tiktoken` for measurements, but the actual platform limit might count differently. The spike needs to determine how the skill is actually counted against the cap.

Finally, they updated documentation across four files to record findings from an external code review. They documented a blind spot in the header-enforcement hook that only watches two file-editing tools, so files created through the shell bypass it silently. They added a stale input to the PRD — `claude-config-repo-audit.md` was deleted but three bullets still depend on pair counts from it. They seeded the verification milestone with ten candidate findings from the external review, marking them to verify rather than accepting them outright. And they corrected a note about the Pre-PR Verification section that had been incorrectly flagged as an extraction candidate — it's duplicated in the testing rules, but this copy sits inside a prompt handed to a separate agent that needs its own definition.

## Key Decisions

- **Measure the fork point between upstream and local skill families before planning the comparison** — Established fork point with verification evidence rather than assertion. Discovered the central assumption was inverted: upstream moved 20 lines across seven months, local moved 419 lines. Replaced planned full line-level diff with targeted approach: upstream's 20 lines get full treatment, local's 419 get section-granularity summary. Freed effort redirected to deep reads of six upstream-only skills and six swarm roles. Script normalizes trailing blank lines and strips only leading frontmatter to avoid measurement artifacts.

- **Confirm project-level skill symlinks are inert across all consuming repositories** — Verified personal-over-project precedence rule by checking both halves. Discovered consumer set is 16 repos, not 9 — seven repos symlink interactive variant and appear in no existing list. All 128 project-level symlinks confirmed inert; zero live.

- **Extract Anki card sourcing strategy into separate skill** — Removed Anki section from both `prd-done` variants to resolve compaction cap overflow. New skill contributes sourcing strategy only (read whole PRD and decision log, dedupe against finished cards first) and delegates all card-making rules to existing `/anki` skill. Resolves token budget issue: `prd-done` interactive dropped from 5,761 to 4,973 tokens, YOLO from 5,582 to 4,986 tokens.

- **Remove restatements of always-loaded rules from prd-done** — Replaced three-channel gh api command block, positive-evidence commit_id test, Fix/Defer/Skip triage rubric, and /code-review explanation with pointers to git-workflow.md. Removed internal redundancy where Critical/Important/Optional taxonomy was defined twice in adjacent bullets.

- **Fix dangling backreference in prd-done after moving CodeRabbit procedures to git-workflow.md** — Line 342 (interactive) and 329 (YOLO) said "Re-run all three `gh api` calls from above" after those calls were moved. Corrected to reference the rule file directly.

- **Correct squash instruction that contradicts always-loaded git rules** — Changed "Clean commit history: Squash or organize commits for clear history" to "Organize commits for clear history — never squash" to resolve contradiction with always-loaded rule "Don't squash git commits".

- **Document the Autonomous Decision Protocol as raw material for consolidation** — Protocol exists only in `prd-next` YOLO variant. Two-category routing: proceed without pausing when task is clearly defined and follows patterns; stop and surface when deviating from spec, architectural implications, ambiguity, falsified assumptions, or out-of-scope effects. Boundary handled explicitly with asymmetric-cost default to caution. Placement at top of file validated by compaction cap measurement — instruction order is a correctness property.

- **Formalize approach for Milestone D2 transcript extraction and redaction** — Committed, re-runnable script should handle enumeration and redaction deterministically, while a model classifies human turns. Completeness claims need mechanization, and manual redaction of transcripts with credentials poses both reproducibility and security risks. Transcript redaction script must include bats test coverage with credential-shape assertion.

## Open Threads

- **Tokenizer measurement disagreement needs resolution** — The completion skill's size fix left the audit within the margin where different tokenizers disagree. Measurements use `tiktoken`, but the actual platform limit may be counted differently. The capability spike milestone needs to determine how the skill body is actually counted against the cap and whether an authoritative count is obtainable.

- **Stale document dependency in PRD 109** — `claude-config-repo-audit.md` was deleted on 2026-08-27, but three bullets in the milestone still depend on pair counts from that document. One of those counts has already been wrong once. Needs verification against the actual tree rather than the outdated table.

- **Ten candidate findings from external code review need verification** — CodeRabbit review surfaced findings ranging from stale status rows to milestone blocks written as pending despite being executed. Rather than accepting these as established, they're marked to verify — treated as a head start on where to look and evidence about drift the PRD accumulates.

- **Header-enforcement hook blind spot** — The hook only watches two file-editing tools, so files created through the shell bypass the check silently. The green result looks identical whether the hook ran and passed or never ran at all.
